\subsection{GRAIL does not win on recall}

GRAIL loses on recall everywhere in this paper. Against SyGMa the margin is significant and
certified: a paired difference of $-0.242$ in mean per-substrate recall, 95\%
confidence interval $[-0.271,-0.212]$. A McNemar test on a different endpoint, whether each
substrate yields any hit at all within the top 15, rejects at $p\approx1.7\times10^{-44}$; it
corroborates the direction rather than testing the margin. It also loses to the learned transformer baselines
on the shared subset of $\nshared$ substrates, where MetaPredictor reaches 0.585 and MetaTrans
0.561 against GRAIL's 0.365 under the same tautomer-aware protocol. Nothing here should be read
as a claim to the state of the art. What the paper contributes is the coverage ceiling, the
decomposition, and the matching protocol; GRAIL is one diagnosed row within them.

\subsection{Precision understates performance}

Every precision number in this paper is a lower bound. Negatives are rule-applicable products
that carry no annotation rather than confirmed non-metabolites, so an unannotated true metabolite
counts as a false positive, and the size of that understatement is unknown. We lead with recall and
with the mean size of the output set for that reason. Precision and F1 nonetheless appear in
\S\ref{sec:metric}, because under a uniform annotation propensity the understatement is a common
factor that leaves the precision ordering identified, and the F1 ordering survives correction down
to an annotation rate the references make implausible.

\subsection{The recommended criterion cannot penalise a limitation of our own system}

The default criterion disregards stereochemistry, and GRAIL's own standardisation writes SMILES
without stereochemistry, so it never emits a stereocentre. Under a stereo-sensitive criterion GRAIL
would be scored on a representation it cannot produce; under the default it is not. On the external
set this is the whole of the difference, and it is large: on the thirteen drugs GRAIL has not
trained on, it rises from $0.059$ to $0.185$ when stereochemistry is disregarded, against a further
$0.038$ from tautomer canonicalisation. (The corresponding figures on all $37$, $0.155$ to $0.310$,
are inflated by training overlap and are superseded below.) GRAIL's external figure is therefore conditional on a tolerance that happens to suit its output
convention. The comparisons in this paper remain valid, because every method is scored
under the same criterion and all three rungs are reported, but a system that does emit
stereochemistry is not rewarded for doing so anywhere in this evaluation.

\subsection{Which number is which}

Recall figures in this paper differ by population, aggregation, truncation and whether duplicate
predictions are collapsed, and several coincide in value without being the same quantity.
Table~\ref{tab:provenance} states each one. Two readings follow from it directly. GRAIL's untruncated and $k{=}15$ figures are identical because it never emits more than fifteen, so
the budget does not bind it. The SyGMa--MetaPredictor gap all but closes between the two
rows, SyGMa ahead by $0.023$ untruncated and by $0.002$ at $k{=}15$, neither margin certified. That
is the budget parameter of \S\ref{sec:metric} acting on the leading pair rather than on ours.

\begin{table}[h]
\caption{Every recall figure this paper reports for the three full-split methods, with what
distinguishes it. \textsc{i-taut} is \textsc{inchikey-tautomer} throughout.}
\label{tab:provenance}
\begin{center}
\footnotesize
\begin{tabular}{llcccc}
\toprule
figure & where & $n$ & aggregation & truncation & collapsed \\
\midrule
GRAIL $\realised$          & Figure~\ref{fig:decomp} & $\ntest$ & micro & $k{=}15$ & no \\
GRAIL $\grailmacro$        & \S\ref{sec:diagnosis}  & $\ntest$ & macro & $k{=}15$ & no \\
GRAIL $0.340$              & Table~\ref{tab:matchsens} & $\ntest$ & macro & $k{=}15$ & yes \\
GRAIL $0.344\pm0.010$      & \S\ref{sec:diagnosis}  & $\ntest$ & macro, 3 seeds & $k{=}15$ & yes \\
GRAIL $0.365$              & Table~\ref{tab:matchsens-subset} & $\nshared$ & macro & $k{=}15$ & yes \\
GRAIL $0.318$              & Appendix~\ref{app:budget} & $\nshared$ & micro & $k{=}15$ & yes \\
\midrule
SyGMa $0.592$              & \S\ref{sec:metric} & $\ntest$ & macro & none & yes \\
SyGMa $0.571$              & Table~\ref{tab:matchsens} & $\ntest$ & macro & $k{=}15$ & yes \\
SyGMa $0.572$              & \S\ref{sec:diagnosis} & $1{,}168$ common & macro & $k{=}15$ & yes \\
SyGMa $0.554$              & Table~\ref{tab:matchsens-subset} & $\nshared$ & macro & $k{=}15$ & yes \\
\midrule
MetaPredictor $0.569$      & \S\ref{sec:metric}, Table~\ref{tab:matchsens} & $\ntest$ & macro & either & yes \\
MetaPredictor $0.585$      & Table~\ref{tab:matchsens-subset} & $\nshared$ & macro & $k{=}15$ & yes \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{Which GRAIL number is which}

Several figures appear and are not interchangeable. On the full split, $0.340$ is the mean of per-substrate recalls after duplicate predictions are
collapsed by the matching criterion, which is what the harness does for every method. $\grailmacro$
is the same mean over the output as emitted, without that collapse. $\realised$ is the pooled ratio
of hits to references, lower because substrates with many references weigh more. The decomposition uses the pooled uncollapsed form, the only one that multiplies out
exactly.

\subsection{The headline is one checkpoint}

The deployed headline, recall@15 of $\realised$ micro and $\grailmacro$ macro, comes from a single
checkpoint rather than an average over seeds. The certification bounds variance in the evaluation
(resampling and discordance over a fixed model), not variance in training across independently
seeded runs. Retraining on three seeds confirms the headline is stable, at $0.344\pm0.010$ macro
and $0.269\pm0.006$ micro, but the headline itself remains a single-checkpoint number.

\subsection{The five-method comparison mixes sample sizes}

\begin{table}[h]
\caption{recall@15 by method and matching criterion on the shared internal subset
($n=\nshared$), with mean number of predictions returned per substrate.}
\label{tab:matchsens-subset}
\begin{center}
\begin{tabular}{lccccc c}
\toprule
\textbf{method} & canon. & InChIKey & no-stereo & Tanimoto$=$1 & tautomer & mean \\
 & & (strict) & (GLORYx) & (fingerprint) & (ours) & outputs \\
\midrule
\textbf{GRAIL}   & 0.356 & 0.357 & 0.358 & 0.356 & 0.365 & \phantom{0}8.7 \\
BioTransformer   & 0.315 & 0.435 & 0.439 & 0.315 & 0.444 & 10.8 \\
SyGMa            & 0.514 & 0.547 & 0.548 & 0.514 & 0.554 & 74.2 \\
MetaTrans        & 0.523 & 0.494 & 0.561 & 0.524 & 0.561 & 12.7 \\
MetaPredictor    & 0.531 & 0.570 & 0.578 & 0.532 & 0.585 & 11.2 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}


\begin{figure}[h]
\begin{center}
\includegraphics[width=0.52\textwidth]{fig_ladder}
\end{center}
\caption{The five criteria as a slope plot on the shared subset. What matters is which lines cross:
BioTransformer passes GRAIL between canonical and InChIKey matching, and MetaTrans passes SyGMa in
the other direction, on predictions that never change.}
\label{fig:ladder}
\end{figure}

The two reversals the main text reports on this subset rest on differences that are individually
indistinguishable from zero at $n=\nshared$. GRAIL against BioTransformer is $[-0.044,+0.122]$
under canonical matching and $[-0.166,+0.007]$ under tautomer; MetaTrans against SyGMa is
$[-0.064,+0.081]$ under canonical and $[-0.130,+0.023]$ under strict InChIKey. What the paper
claims is the interaction that produces them; the reversals themselves illustrate it and are not
evidence on their own. Of the two interactions, only GRAIL against BioTransformer survives the
correction at the family the search spanned (Appendix~\ref{app:holm}); MetaTrans against SyGMa is
reported as an uncorrected interval.

The three additional comparators (BioTransformer, MetaPredictor and MetaTrans) are scored
only on the shared subset of $\nshared$ substrates for which frozen third-party predictions exist,
whereas GRAIL's and SyGMa's headline numbers elsewhere are reported on the full clean test split of
$\ntest$. Within the comparison table itself all five methods are re-scored on the same $\nshared$
substrates, so the columns are paired on identical inputs.

MetaPredictor has since been re-scored on $1{,}169$ of the test substrates, at recall@15 of 0.568, close to
SyGMa, which indicates its subset value is representative. BioTransformer and MetaTrans remain on
the subset. A full five-method rerun at a single sample size is future work: BioTransformer's
subset configuration was not scripted, and MetaTrans's inference pipeline no longer reproduces
in-tree.

\subsection{The five-method comparison under four aggregates}

Table~\ref{tab:setmetrics} re-scores every method's deployed output set on the shared subset, with
no top-$k$ truncation, since precision and Jaccard are set quantities. Its output sizes count
structures as emitted, whereas Table~\ref{tab:matchsens-subset} counts them after the harness
collapses duplicates under the matching criterion, which is why the two differ slightly for every
method that emits a duplicate. The orderings by recall and
by F1 are different orderings.

\begin{table}[h]
\caption{Macro precision, recall, F1 and Jaccard over each method's deployed output on the shared
subset ($n=\nshared$), under \textsc{inchikey-tautomer}. Rank by recall and rank by F1 in the last
two columns.}
\label{tab:setmetrics}
\begin{center}
\begin{tabular}{lccccc cc}
\toprule
\textbf{method} & prec. & recall & F1 & Jaccard & outputs & rk-R & rk-F1 \\
\midrule
MetaTrans        & 0.122 & 0.565 & \textbf{0.177} & 0.105 & 11.5 & 2 & 1 \\
MetaPredictor    & 0.116 & \textbf{0.585} & 0.174 & 0.101 & 10.9 & 1 & 2 \\
BioTransformer   & 0.127 & 0.453 & 0.162 & 0.104 & 10.7 & 4 & 3 \\
\textbf{GRAIL}   & 0.110 & 0.365 & 0.143 & 0.092 & \phantom{0}8.7 & 5 & 4 \\
SyGMa            & 0.029 & 0.562 & 0.051 & 0.028 & 74.2 & 3 & 5 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Only one method moves with certification, and it is the one the decomposition names. On F1 the four
selecting methods are mutually indistinguishable, every pairwise interval covering zero, the widest
separation being GRAIL against MetaTrans at $-0.033$ $[-0.071,+0.004]$. SyGMa is below all four,
$+0.092$ $[+0.063,+0.123]$ against GRAIL rising to $+0.125$ $[+0.103,+0.147]$ against MetaTrans.
On recall it is above two of them, $-0.109$ $[-0.168,-0.052]$ against BioTransformer and $-0.197$
$[-0.274,-0.120]$ against GRAIL. Those two pairs are the certified reversals: each is significant
in one direction on recall and in the other on F1.

The remaining exchanges of place, MetaTrans past MetaPredictor at the top and BioTransformer past
GRAIL below it, are within noise and we do not claim them. The reading is not that the aggregate
shuffles a leaderboard arbitrarily. It is that the aggregate is decisive for exactly the
architecture the factorisation identifies, the one with no selection stage, and not detectably
different among the architectures that select. That last is a failure to detect rather than a
demonstration of absence: the widest of those intervals still admits a difference of $0.07$ at
$n=\nshared$, and separating the two readings needs a larger shared subset than the comparators
currently supply.

\subsection{How much of the ordering is real}
\label{app:orderingstability}

The reversals above are reported one at a time, which answers whether the phenomenon exists and not
how much of the leaderboard it consumes. The aggregate is available from the same frozen predictions
on the same $\nshared$ substrates: every pairing of a matching criterion with an aggregate is a
leaderboard someone could have published, all five criteria are in use in this literature and all
four aggregates are reported in it, and not one of the twenty cells is declared by the paper that
stands on it.

\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
what is allowed to vary & cells & orderings & pairs changing sign & certified both ways \\
\midrule
criterion and aggregate            & $20$ & $11$ & $9/10$ & $2/10$ \\
criterion alone, at recall         & $5$  & $3$  & $2/10$ & $0/10$ \\
criterion alone, at F1             & $5$  & $3$  & $5/10$ & $0/10$ \\
aggregate alone, at one criterion  & $4$  & $4$  & $5/10$ & $1/10$ \\
\bottomrule
\end{tabular}
\end{center}

The two counts differ by more than a factor of four and only the smaller one is a finding. Five
methods take eleven distinct orderings across the twenty cells and nine of the ten pairwise
comparisons change sign somewhere in them, which is a description; two pairs reverse with disjoint
intervals in both directions, and they are the two named above. Quoting the first without the second
is the fallacy \S\ref{sec:rankflip} rejects, and it is available here to anyone who wants it,
ourselves included.

Which axis does the work is visible in the slices, and it is the one \S\ref{sec:metric} says it is.
Holding the aggregate at recall, the criterion alone moves two pairs and certifies none: it erases
gaps rather than reordering. Holding the criterion fixed, the aggregate alone yields four distinct
orderings from four cells --- a different leaderboard for every aggregate --- and certifies one. The
count of orderings is not a property of how finely a sweep was gridded, since both axes here are
enumerable and small; what a finer budget sweep would add is more cells, not more information.

\subsection{Four methods, one population, and the budget that reorders all of them}
\label{app:fourmethod}

The reorderings in \S\ref{sec:metric} run through one method that emits far more than the others,
which invites the reading that the finding is about SyGMa rather than about budgets. A second
high-output method settles it. The SMIRKS variant of MetaTox supplies predictions for $291$ test
substrates, every one of which carries references and predictions from all four methods, so the
comparison needs no new population beyond naming it. Scored through the same matcher, micro, with
each method's own parent and its duplicates dropped before the budget is applied:

\begin{center}
\small
\begin{tabular}{lcccccc}
\toprule
method & emitted & r@$5$ & r@$10$ & r@$15$ & r@$30$ & r@$50$ \\
\midrule
SyGMa          & $72.98$ & $0.3744$ & $0.4556$ & $0.4842$ & $0.5068$ & $0.5113$ \\
MetaTox        & $33.6$ & $0.218$ & $0.388$ & $0.5143$ & $0.6015$ & $0.6271$ \\
MetaPredictor  & $10.75$ & $0.3549$ & $0.4707$ & $0.4797$ & $0.4797$ & $0.4797$ \\
GRAIL          & $10.04$ & $0.2286$ & $0.2526$ & $0.2647$ & $0.2677$ & $0.2677$ \\
\bottomrule
\end{tabular}
\end{center}

The four methods take three distinct orderings across the sweep, and the method that moves
furthest is the new one: MetaTox is last of the four at every budget up to $5$ and first of them
from $15$ on --- which is the budget this field reports at. Nothing about the chemistry changes
between those columns. At $k=5$ it has been truncated to a seventh of what it emits, and at $k=30$
it has not. Its predictions are ranked by its own metabolite-likeness score, the quantity the
delivery is named for; PASS writes that score only for the $5{,}178$ of $10{,}603$ predictions it
declares above its own threshold, and the rest keep the order the file gives them, since PASS
expresses no preference among predictions it declines to score.

Each method's own parent is dropped from its ranked list before the budget applies, and the choice
runs in our favour rather than against it. GRAIL returns the substrate itself on $80$ of the $291$,
MetaPredictor on $52$ and MetaTox on $14$; SyGMa's zero is a property of the released artifact
rather than of SyGMa, whose tree is rooted at the substrate and whose parent was stripped when those
predictions were frozen. A slot spent on the parent cannot score, so dropping it helps whichever
method spends one, which is ours most of all. It does not carry the result: with the parent left in,
the four methods take the same three orderings at the same budgets and GRAIL's recall@$15$ moves
from $0.265$ to $0.263$.

\subsection{The multiplicity correction, and the family it is taken over}
\label{app:holm}

\S\ref{sec:rankflip} corrects for multiplicity post hoc, and both inputs to that correction belong
here rather than in a sentence. The p-values are normal approximations to the stored paired-bootstrap
intervals, $\mathrm{se}=(\mathrm{hi}-\mathrm{lo})/2z$; the intervals themselves remain the primary
evidence and the approximation is adequate only because most decisions sit far from their threshold
--- the one that does not is stated as such below.

We declare the family conservatively as the grid the two axes admit rather than the comparisons we
report; no family was pre-specified. The two axes are method pairs and criterion steps, and two steps are reported on each set:
canonical$\rightarrow$tautomer and canonical$\rightarrow$InChIKey internally,
InChIKey$\rightarrow$no-stereo and no-stereo$\rightarrow$tautomer externally. Six pairs times two
steps is a family of twelve externally; ten pairs times two steps is twenty internally. Taking the
family as the pairs actually tested, or as the pairs one table admits, counts only one of the two
axes.

\begin{table}[h]
\caption{Every comparison entering a family, with its raw p-value, its Holm-adjusted
p-value at the searched family size, and the step-down threshold its rank carries at
$\alpha=0.05$. Internal pairs are the $\nshared$-substrate subset, external pairs the $\nglory$
GLORYx drugs, and the last row is the second confirmatory comparison, on $245$ test substrates.}
\label{tab:holm}
\begin{center}
\footnotesize
\begin{tabular}{llcccl}
\toprule
family & interaction & raw $p$ & Holm $p$ & threshold & decision \\
\midrule
external ($m{=}12$) & MetaPredictor vs SyGMa          & $1.13\times10^{-4}$ & $1.4\times10^{-3}$ & $0.0042$ & rejected \\
                    & GRAIL vs SyGMa                  & $1.65\times10^{-4}$ & $1.8\times10^{-3}$ & $0.0045$ & rejected \\
                    & BioTransformer vs GRAIL         & $7.07\times10^{-4}$ & $7.1\times10^{-3}$ & $0.0050$ & rejected \\
                    & BioTransformer vs MetaPredictor & $3.80\times10^{-3}$ & $3.4\times10^{-2}$ & $0.0056$ & rejected \\
                    & BioTransformer vs SyGMa         & $0.125$             & $0.996$            & $0.0063$ & not rejected \\
                    & MetaPredictor vs GRAIL          & $0.537$             & $1.000$            & $0.0071$ & not rejected \\
\midrule
internal ($m{=}20$) & GRAIL vs BioTransformer         & $1.65\times10^{-6}$ & $3.3\times10^{-5}$ & $0.0025$ & rejected \\
                    & MetaTrans vs SyGMa              & $3.35\times10^{-3}$ & $6.4\times10^{-2}$ & $0.0026$ & not rejected \\
\midrule
selection ($m{=}1$) & learned selector vs frequency prior & $2.0\times10^{-8}$ & $2.0\times10^{-8}$ & $0.0500$ & rejected \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The external family is unaffected by the widening: the same four interactions are rejected at
$m=6$ and at $m=12$, and the two that fail are nowhere near their thresholds at either size. The
internal one is not. At $m=2$ or $m=10$ both internal interactions survive; at the family the
search actually spanned, the GRAIL--BioTransformer interaction survives with room to spare while
MetaTrans--SyGMa misses its threshold of $0.0026$ at $p=0.00335$. We report that second interaction
as an uncorrected interval from here on and do not count it as certified. This is the one decision
close enough to its threshold that the normal approximation matters, and it is decided against us,
which is the direction in which an approximation is safe to use.

\paragraph{The same endpoint on a scale-free footing.} The endpoint is a paired difference of raw
gains on a $[0,1]$-bounded metric, and the arms enter the relaxation step from different baselines.
Internally that confound runs against the effect and \S\ref{sec:rankflip} says so; externally it
runs \emph{with} it, MetaPredictor entering at $0.362$ against SyGMa's $0.492$ and gaining the more.
Deploying the argument only where it exculpates would not do, so the obvious scale-free version is
reported here: the gain as a fraction of the headroom left, $\Delta/(1-\text{recall}_{\text{strict}})$,
resampled paired over substrates so numerator and denominator move together.

\begin{center}
\begin{tabular}{lccc}
\toprule
method & recall (strict) & raw gain & normalised gain (95\% CI) \\
\midrule
\multicolumn{4}{l}{\emph{external, $n=\nglory$, \textsc{inchikey}$\rightarrow$\textsc{inchikey-tautomer}}} \\
SyGMa          & 0.492 & $+0.007$ & $+0.013$ $[+0.000,+0.042]$ \\
BioTransformer & 0.346 & $+0.028$ & $+0.043$ $[+0.004,+0.099]$ \\
MetaPredictor  & 0.362 & $+0.142$ & $+0.222$ $[+0.125,+0.333]$ \\
GRAIL          & 0.155 & $+0.178$ & $+0.210$ $[+0.117,+0.313]$ \\
\midrule
\multicolumn{4}{l}{\emph{internal, $n=\nshared$, \textsc{canonical}$\rightarrow$\textsc{inchikey-tautomer}}} \\
GRAIL          & 0.356 & $+0.010$ & $+0.015$ $[+0.001,+0.040]$ \\
BioTransformer & 0.315 & $+0.130$ & $+0.189$ $[+0.121,+0.262]$ \\
\bottomrule
\end{tabular}
\end{center}

Every decision in Table~\ref{tab:holm} survives the change of scale, in both directions. Signing each
interaction as the second method's normalised gain minus the first's, exactly as that table's pairs
are ordered, the four externally rejected ones keep their sign and exclude zero:
MetaPredictor--SyGMa $-0.209$ $[-0.322,-0.111]$, GRAIL--SyGMa $-0.197$ $[-0.305,-0.095]$,
BioTransformer--GRAIL $+0.168$ $[+0.069,+0.273]$, BioTransformer--MetaPredictor $+0.179$
$[+0.075,+0.297]$, and internally GRAIL--BioTransformer $+0.174$ $[+0.108,+0.245]$. The two
externally not rejected stay indistinguishable from zero, BioTransformer--SyGMa $-0.029$
$[-0.083,+0.003]$ and MetaPredictor--GRAIL $-0.012$ $[-0.163,+0.141]$. GRAIL's external row
is on all $\nglory$ here, where the contaminated arm inflates it, and is not the $13$-drug figure
\S\ref{sec:rankflip} reports; it is shown because the normalisation is what is at issue, not the
level. The raw paired difference remains the endpoint --- it is a differential re-scoring of frozen
predictions and is well defined --- and this is a robustness column beside it.

The last row of Table~\ref{tab:holm} is the second comparison \S\ref{sec:tame} declares confirmatory, and it is not a
protocol $\times$ method interaction, so it belongs to neither family above: the learned-versus-prior
rule-selection gap of \S\ref{sec:xmc}, $-0.144$ $[-0.196,-0.095]$ macro at $k=15$ on $245$ test
substrates. No family was pre-specified for it either, and
results/prior\_vs\_learned.json spans three selector modes and two stages, so a conservative family
here is not of size one. Saying so is the answer rather than an evasion: a
comparison the paper declares corrected has to appear here with its threshold, and at
$p=2.0\times10^{-8}$ against $0.05$ the correction changes nothing whatever family it is put in.

\subsection{External validity rests on 37 substrates}

The external ceiling is measured on 37 GLORYx parent substrates, so its 95\% interval is wide by
construction, at $[0.616,0.804]$. The regression that partially explains the gap between internal
and external coverage is fit on the same small external population. We report it as suggestive
rather than as a law that transfers.

\subsection{One assumption behind Proposition 2 is not measured}

Proposition 2's explanation assumes that the probability of a rule being labelled is proportional
to its marginal firing rate. That propensity model is asserted rather than estimated from data.
The identifiability argument for why a learned selector underperforms a frequency prior is
therefore consistent with the stated mechanism, not proof of it.

\subsection{Proposition 3 bounds single-step application only}

Depth-2 application recovers 0.012 of ceiling at 8.5 times the candidate cost, so the bound
constrains single-step rule application rather than metabolite prediction in general. Multi-step
chemistry and chemistry outside the bank remain open sources of coverage.

\subsection{The factorised generator, measured in the convention it runs in}

The factorised redesign of the generator reaches recall@15 of $0.489$ $[0.464,0.514]$ on the full
test split against the deployed $\grailmacro$, at $14.0$ candidates per substrate against $8.4$.
That pair is not budget-matched, so what we claim is the conservative reading: truncated to five
candidates it matches $\grailmacro$ while emitting fewer than half as many. It also contributes a
re-ranking gain of $+0.0165$, 95\% confidence interval $[0.006,0.027]$, on the full clean test.
Closing the larger gap to SyGMa still requires a broader rule bank, which is outside the scope of
this paper.

\subsection{The rank-flip result, and the run that decides whether it leaves metabolism}
\label{app:xdomainflip}

The test was specified before it could be run, and specified so that it could fail: re-score the
frozen predictions of at least two comparable published models in another domain under the same
conventions, and if the ordering does not move, with an interaction interval covering zero, the
broader claim fails.

That run needs at least two models of comparable strength whose match sensitivity differs and
whose raw ranked predictions are public. Such predictions are public: the EvalRetro benchmark
releases the ranked outputs of twelve single-step retrosynthesis systems on what it presents as the
USPTO-50k test set \citep{Hastedt_2024}, eleven of them as CSV, under a licence that permits
re-scoring. Nothing needs training, and no checkpoint needs to run.

\paragraph{The eleven are not on one test set, and that decides what can be compared.}
Reading the released files rather than their table shows three disjoint populations. Clustering the
eleven by which products they contain, the within-cluster overlap is $1.00$ and the between-cluster
overlap is $0.10$, which leaves no room for a judgement call:

\begin{center}
\footnotesize
\begin{tabular}{lccc}
\toprule
group & systems & reactions agreed & share of its products in our split \\
\midrule
seven-system  & 7 & $4{,}999$ & $0.10$ \\
three-system  & 3 & $4{,}982$ & $1.00$ \\
one-system    & 1 & $4{,}998$ & $0.10$ \\
\bottomrule
\end{tabular}
\end{center}

A leaderboard is a leaderboard only within a group, so we do not assemble the eleven into one table
and neither should a reader of the released files. Two consequences follow and we take both. The
comparison below runs on the seven-system group, reconstructing its test set from the reactions all
seven carry and agree the answer to, rather than trusting any one file's ordering or its index
column --- which means a running prediction counter in one file, a reaction index in another and a
float in a third. And our own USPTO-50k split turns out to be the three-system group's, so the
single-model probe reported later in this appendix is measured on that set and not on the larger
one; we say which set each number is on rather than letting the shared name imply they are the same
population.

\paragraph{The run comes back positive, and at the budget this field headlines.} Seven systems,
$4{,}999$ reactions, frozen predictions, nothing trained. Top-1 accuracy under each convention:

\begin{center}
\footnotesize
\begin{tabular}{lcccc}
\toprule
system & canonical & \textsc{inchikey} & no-stereo & tautomer \\
\midrule
Graph2SMILES & 0.5293 & 0.5293 & \textbf{0.5571} & \textbf{0.5595} \\
GraphRetro   & \textbf{0.5377} & \textbf{0.5377} & 0.5501 & 0.5555 \\
Retroformer  & 0.5325 & 0.5325 & 0.5541 & 0.5559 \\
LocalRetro   & 0.5339 & 0.5339 & 0.5469 & 0.5495 \\
GLN          & 0.5247 & 0.5247 & 0.5377 & 0.5397 \\
G2Retro      & 0.5147 & 0.5147 & 0.5291 & 0.5309 \\
RetroXpert   & 0.4467 & 0.4467 & 0.4619 & 0.4631 \\
\bottomrule
\end{tabular}
\end{center}

The leader changes. Under canonical matching --- and identically under \textsc{inchikey}, which on
these predictions separates nothing that canonical SMILES does not --- the order runs GraphRetro,
LocalRetro, Retroformer, Graph2SMILES. Disregard stereochemistry and Graph2SMILES goes from fourth
to first, with Retroformer second and GraphRetro third. Five of the twenty-one pairs exchange, on
predictions that never change.

The exchange is the visible part; the interaction is the claim, and it is certified under the
strictest correction this paper applies anywhere. Of $448$ paired interaction tests --- every pair
of systems, every pair of conventions, every $k$ --- $233$ have intervals excluding zero and
\textbf{$109$ survive Holm at $0.05$ across the whole family}. The largest is Graph2SMILES against
GraphRetro between canonical and stereo-blind matching, $-0.0154$ $[-0.020,-0.011]$.

Two qualifications travel with it. The effect is concentrated at top-1: at $k=3$, $5$ and $10$ the
seven take a single ordering under all four conventions and no pair exchanges. And what moves the
ordering here is stereochemistry, with tautomer canonicalisation adding little on top --- the same
asymmetry the external metabolite set shows, and the opposite of the internal one. Top-1 is the
number this field leads with, so a criterion that reorders only there reorders the reported
leaderboard; but the honest statement is that the reordering is a top-1 phenomenon on this
benchmark, not a property of the whole accuracy curve.

Two earlier measurements outside metabolism come back negative, and they are negative about
different things. The first carries a leaderboard: it scores eight generative models and can
therefore exhibit an exchange or fail to. The second carries one model, and one model has no
ordering to disturb, so it cannot test reordering at all --- what it measures is how far the
convention moves a score in that domain, which is an upper bound on what any pair separated by more
than that could suffer, and a bound is not an observation. We keep it for the bound and label it as
one.

The first targets molecular generation, where the matching criterion is not a way of checking an
answer but the metric itself: MOSES reports Unique@$k$ as the count of distinct canonical SMILES
among a model's generations \citep{Polykovskiy_2020}. A model that emits a keto and an enol form of
one compound is credited with two molecules. We re-scored the published generations of all eight
models MOSES ships samples for, 240{,}208 molecules, under the same five criteria.

Tautomer matching is inert on this set. Every one of the eight returns its canonical Unique@10{,}000
to within $0.0001$, the single non-zero difference being JT-VAE's $0.9992$ against $0.9991$. The
relaxation that does something is stereochemistry, and what it does is small: the hidden Markov
baseline falls $0.923 \to 0.888$ and the $n$-gram baseline $0.986 \to 0.957$, while four of the
remaining six move by $0.001$ or less and a fifth by $0.0011$. That is enough for one rank exchange, LatentGAN dropping
$0.998 \to 0.988$ past a combinatorial baseline that does not move from $0.990$, so two adjacent
mid-table models swap positions 5 and 6 under stereo-blind matching and under no other criterion.

We report this as a negative control rather than a null. The ordering is stable under four of five
criteria and the one exchange it admits is between models $0.002$ apart, against a metabolism
result in which a significant advantage between two published tools is erased outright. The reason
is visible in the numbers: seven of the eight sit at $0.986$ or above, so there is little room to
collapse anything, and generators trained on canonical SMILES from ZINC reproduce that canonical
form. The asymmetry is the same one the external metabolite set shows, stereochemistry doing the
work and tautomerism doing none; what differs between the domains is how much is at stake.

The second targets retrosynthesis. Scoring ReactionT5v2 on 200
substrates from the USPTO-50k test set, the same predictions move only about 0.02 in top-1
accuracy across canonical, stereo-stripped, InChIKey and tautomer matching, between 0.695 and
0.715, roughly an order of magnitude less than the interaction we measure in metabolism. A
plausible reason is that retrosynthesis references are canonical, clean small molecules, whereas a
metabolism rule engine emits tautomer and stereochemical variants of its references. What this
licenses is narrow, and the seven-system result above says how narrow. A leaderboard reorders when
the convention moves two methods by \emph{different} amounts, and one model cannot show a
differential. It is tempting to read one model's small movement as a bound on what a pair could
suffer, and that reading is arithmetically true and practically empty: the movement here is about
$0.02$, the seven published systems sit within $0.005$ of one another, and five pairs exchange. A
bound larger than the gaps it is supposed to protect protects nothing.

\paragraph{What the three measurements say together.} Matching conventions do not reorder every
leaderboard, and the condition is a property of the references rather than of the task --- but it is
narrower than we first wrote it. The effect needs references carrying representational variety
\emph{of a kind the compared systems differ on}. Generative models trained on canonical ZINC SMILES
reproduce that canonical form, so there is nothing to diverge from and the ordering barely moves.
Retrosynthesis references are canonical, which is why the earlier single-model probe found so
little, but they carry stereochemistry and the systems differ in reproducing it --- and that alone
reorders the top-1 table. Metabolite references carry tautomeric \emph{and} stereochemical variety,
written with whatever the reporting assay produced, and there the effect is largest. Canonical is
not the same as variety-free, which is the distinction our own earlier reading missed.

\subsection{The external set is not external to GRAIL}

The criterion ladder of \S\ref{sec:rankflip} is reported on the $\nglory$ GLORYx drugs as an
external replication. Checking two independent curations against each other turned up something
that check was not looking for: $19$ of those $37$ parents are substrates in GRAIL's training split
and $5$ more are in validation. Thirteen are unseen. The overlap costs a minute to compute and had
never been computed.

The contamination is one-sided, which decides how much has to be repaired. SyGMa and BioTransformer
do not train on this corpus, and MetaPredictor was trained by its authors on their own data, so
their figures are unaffected \emph{by this overlap} and stay at $n=\nglory$. Only GRAIL's are
inflated, and inflated in our
own favour. Restricting the other three to the thirteen would have been an error in the opposite
direction, discarding two thirds of the sample to correct a bias they do not have.

The audit is scoped to GRAIL's splits because that is the only overlap that can inflate a number in
this paper, and one might still ask whether a learned comparator had seen these well-known drugs.
The reported effect is the wrong shape for that: a method that had memorised stereo-bearing
reference structures would match them under strict \textsc{inchikey} and have \emph{less} left for a
stereo-blind criterion to recover, so memorisation attenuates a stereo-step gain rather than
producing one. MetaPredictor enters that step at $0.362$, below the rule engine's $0.492$, which is
the opposite signature. Nor is the comparator arm a system that trained on nothing: SyGMa's rule
probabilities are themselves fitted to an observed-metabolite database \citep{Ridder_2008}.

Recomputing on the thirteen with the same loaders, matcher and frozen predictions, so that only the
substrate list changes, GRAIL's external recall falls by up to a factor of $2.6$: $0.155 \to 0.059$
under strict \textsc{inchikey}, $0.310 \to 0.185$ once stereochemistry is disregarded, $0.333 \to
0.223$ under the tautomer-aware default. The other three methods move by less than $0.07$, which is
the sample change rather than a correction.

Two claims survive and one does not. GRAIL's stereo step remains significant at $+0.126$
$[+0.028,+0.239]$ against $+0.155$ on the full set, so the mechanism is real rather than memorised:
GRAIL cannot emit a stereocentre, and a stereo-blind criterion rewards that whether or not it has
seen the drug. Its interaction with SyGMa survives too, but carries no information beyond that step
and should not be counted as a second result: SyGMa's own stereo step on these thirteen is exactly
zero, with an interval of $[0,0]$, so the interaction is numerically the same measurement,
$+0.126$ $[+0.028,+0.239]$. The
interaction between BioTransformer and GRAIL does not: $+0.150$ $[+0.069,+0.242]$ on all $\nglory$,
it is $-0.104$ $[-0.223,+0.000]$ on the unseen drugs, reversing sign and losing significance. It is
among the two largest on the full set and is not claimed on either. Of six pairs, four were significant on the full set
and two are on the clean one, both against SyGMa.

The headline external result is unaffected because it involves neither contaminated arm. Under
strict \textsc{inchikey} SyGMa leads MetaPredictor by $-0.130$ $[-0.196,-0.065]$ and one rung later
the advantage is gone; both methods are external to this corpus in the ordinary sense.

We report this rather than quietly recomputing because the failure is instructive about the protocol
the paper argues for. An audit of train-test overlap is cheap, is not implied by any of the other
checks we run, and surfaced here only as a by-product of measuring something else.

\subsection{What two independent curations of the same drug agree on}

Twenty-five of the external drugs are also curated by this paper's corpus, independently, which
makes their two metabolite lists comparable to each other in exactly the way a prediction is
compared to a reference. Nothing in this subsection involves a model.

\begin{figure}[h]
\begin{center}
\includegraphics[width=0.52\textwidth]{fig_curators}
\end{center}
\caption{Two independent expert curations of the same twenty-five drugs, scored against each other
under each criterion. No model is involved. The same two lists agree about a seventh of their
content under the strictest rule and two-fifths under the most tolerant.}
\label{fig:curators}
\end{figure}

The criterion moves that agreement by a factor of nearly three. Under strict \textsc{inchikey} the
two curations of the same drug reach a Jaccard of $0.145$; under canonical SMILES,
which this harness keys stereo-blind, $0.241$; under Tanimoto$=$1, $0.239$; stereo-blind
\textsc{inchikey}, $0.384$; and under the tautomer-aware default $0.406$. The five are not
nested for this measurement --- canonical sits above strict \textsc{inchikey}, because dropping
stereochemistry from a SMILES string merges more than dropping it from a hash --- so what the figure
shows is the span, not a ladder. One-sided,
the corpus recovers $0.647$ $[0.535,0.756]$ of the GLORYx list and GLORYx $0.539$ $[0.439,0.643]$ of
the corpus list, both under the default. All figures are on the same twenty-five drugs, so none of
this is a population effect. Two experts looking at the same chemistry appear to agree about
a seventh of it under the strictest criterion and about two-fifths under the most tolerant; the
difference is notation, not chemistry, and it is the cleanest argument in this paper for declaring
the criterion.

Those two numbers also bound what the benchmark can register, and the bound needs no model. Neither
curation is a predictor --- a source's list is what its inclusion criteria admitted, not an attempt
to reconstruct the other's --- so what the pair fixes is the level at which two independent readings
of the same chemistry coincide: $0.539$ one way, $0.647$ the other. References that no method
retrieves cannot all be counted as model error.

Where models sit against that level is a statement about budget, and the budget is the parameter
\S\ref{sec:metric} declares. Uncapped, scored against the corpus annotation with the same matcher,
MetaPredictor recovers $0.562$ $[0.443,0.676]$, SyGMa $0.524$, BioTransformer $0.480$, GRAIL
$0.338$; MetaPredictor's paired gap to the curation is $-0.023$ $[-0.153,+0.123]$, a tie inside
noise rather than a lead, and it is bought with $10.6$ metabolites per drug against the curation's
$6.0$, SyGMa's near-tie with $95.9$. Capped instead at each drug's own curated set size --- the
budget the curation actually spent, with the curation itself not truncated --- its $0.539$ stands
against MetaPredictor's $0.301$, SyGMa's $0.271$, GRAIL's $0.284$ and BioTransformer's $0.193$, and
every paired gap excludes zero (MetaPredictor $+0.238$ $[+0.123,+0.354]$, the smallest of the four).
A fixed $k{=}6$, the curation's mean output, truncates the curation as well and points the same way
without reaching significance: $0.472$ against $0.394$, gap $+0.078$ $[-0.050,+0.210]$. It is the
more conservative of the two matches, since a curation asserts no ranking and the order its list is
cut in is not one it claims.

Models therefore reach curator-level recall only by spending more output than a curator does. At the
curator's own budget, matched at each drug's own curated set size, they are behind by a quarter to a
third of the reference set; the symmetric $k{=}6$ match in the sentence before this one points the
same way and does not reach significance, and this verdict does not cover it.

That per-substrate cap is not an arbitrary handicap, which is the objection it invites. The size
supplied is the curator's own, so it is the per-substrate cardinality an oracle would choose, and
Appendix~\ref{app:props} measures what that oracle is worth and how little of it is obtainable. Both
figures are taken out of sample, from the same cross-fit, so they share a baseline: for
MetaPredictor on the full split the oracle cutoff lifts macro-F1 from $0.254$ at the best constant
$k$ to $0.435$, and a head trained to predict that cutoff recovers $0.008$ of the $0.181$ available,
$4.3\%$ ($3.2\%$ for GRAIL, $1.1\%$ for SyGMa). No method here can set this budget for itself. What
the cap does \emph{not} do is deny the model outputs it could have justified --- under recall a cap
can only cost --- so the comparison reduces to the plain statement: spending what the curator spent,
the curator retrieves $0.539$ of the other source's list and the best model $0.301$.

One reading is not available, and it is the one the framing invites. The curators had the literature
on each of these drugs in front of them; they were recording what had been observed, not predicting
it from structure. So this measures the references, not the contestants: an independent competent
reading of the same chemistry omits $46\%$ of what the corpus records, which is why recall past that
level is increasingly a measurement of one source's inclusion criteria. It is not evidence about
whether models have caught up with expert judgement, and should not be read as any.

Two limits are worth stating rather than leaving to be found. Twenty-five drugs give intervals
around $\pm0.1$, and these are drugs two sources both chose to curate, so they carry $5.7$ to $6.5$
references each against $2.2$ across the corpus. Better-studied compounds are exactly where two
curations have the most opportunity to agree, so $0.539$ is more likely an upper bound on
inter-source agreement than a typical value. Which way that biases the ceiling we cannot say: on
less-studied chemistry both the curations and the models have less to go on, and we have not
measured which of the two degrades faster.
