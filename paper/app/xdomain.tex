\subsection{GRAIL does not win on recall}

GRAIL loses on recall everywhere in this paper. Against SyGMa the margin is significant and
certified: a paired difference of $-0.242$ in mean per-substrate recall, 95\%
confidence interval $[-0.271,-0.212]$. A McNemar test on a different endpoint, whether each
substrate yields any hit at all within the top 15, rejects at $p\approx1.7\times10^{-44}$; it
corroborates the direction rather than testing the margin. It also loses to the learned transformer baselines
on the shared subset of $\nshared$ substrates, where MetaPredictor reaches 0.585 and MetaTrans
0.561 against GRAIL's 0.365 under the same tautomer-aware protocol. Nothing here should be read
as a claim to the state of the art. What the paper contributes is the coverage ceiling, the
decomposition, and the matching protocol; GRAIL is one diagnosed row within them.

\subsection{Precision understates performance}

Every precision number in this paper is a lower bound. Negatives are rule-applicable products
that carry no annotation rather than confirmed non-metabolites, so an unannotated true metabolite
counts as a false positive, and the size of that understatement is unknown. We lead with recall and
with the mean size of the output set for that reason. Precision and F1 nonetheless appear in
\S\ref{sec:metric}, because under a uniform annotation propensity the understatement is a common
factor that leaves the precision ordering identified, and the F1 ordering survives correction down
to an annotation rate the references make implausible.

\subsection{The recommended criterion cannot penalise a limitation of our own system}

The default criterion disregards stereochemistry, and GRAIL's own standardisation writes SMILES
without stereochemistry, so it never emits a stereocentre. Under a stereo-sensitive criterion GRAIL
would be scored on a representation it cannot produce; under the default it is not. On the external
set this is the whole of the difference, and it is large: GRAIL rises from 0.155 to 0.310 when
stereochemistry is disregarded, against a further 0.023 from tautomer canonicalisation. A reader
should therefore treat GRAIL's external figure as conditional on a tolerance that happens to suit
its output convention. The comparisons in this paper remain valid, because every method is scored
under the same criterion and all three rungs are reported, but a system that does emit
stereochemistry is not rewarded for doing so anywhere in this evaluation.

\subsection{Which GRAIL number is which}

Several figures appear and are not interchangeable. On the
full split, $0.340$ is the mean of per-substrate recalls after duplicate predictions are collapsed
by the matching criterion, which is what the re-scoring harness does for every method;
$\grailmacro$ is the same mean over the output as emitted, without that collapse; and $\realised$
is the pooled ratio of hits to references, lower because substrates with many references weigh
more. The decomposition uses the pooled uncollapsed form, the only one that multiplies out
exactly.

\subsection{The headline is one checkpoint}

The deployed headline, recall@15 of $\realised$ micro and $\grailmacro$ macro, comes from a single
checkpoint rather than an average over seeds. The certification bounds variance in the evaluation
(resampling and discordance over a fixed model), not variance in training across independently
seeded runs. Retraining on three seeds confirms the headline is stable, at $0.344\pm0.010$ macro
and $0.269\pm0.006$ micro, but the headline itself remains a single-checkpoint number.

\subsection{The five-method comparison mixes sample sizes}

\begin{table}[h]
\caption{recall@15 by method and matching criterion on the shared internal subset
($n=\nshared$), with mean number of predictions returned per substrate.}
\label{tab:matchsens-subset}
\begin{center}
\begin{tabular}{lccccc c}
\toprule
\textbf{method} & canon. & InChIKey & no-stereo & Tanimoto$=$1 & tautomer & mean \\
 & & (strict) & (GLORYx) & (fingerprint) & (ours) & outputs \\
\midrule
\textbf{GRAIL}   & 0.356 & 0.357 & 0.358 & 0.356 & 0.365 & \phantom{0}8.7 \\
BioTransformer   & 0.315 & 0.435 & 0.439 & 0.315 & 0.444 & 10.8 \\
SyGMa            & 0.514 & 0.547 & 0.548 & 0.514 & 0.554 & 74.2 \\
MetaTrans        & 0.523 & 0.494 & 0.561 & 0.524 & 0.561 & 12.7 \\
MetaPredictor    & 0.531 & 0.570 & 0.578 & 0.532 & 0.585 & 11.2 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}


The two reversals the main text reports on this subset rest on differences that are individually
indistinguishable from zero at $n=\nshared$. GRAIL against BioTransformer is $[-0.044,+0.122]$
under canonical matching and $[-0.166,+0.007]$ under tautomer; MetaTrans against SyGMa is
$[-0.064,+0.081]$ under canonical and $[-0.130,+0.023]$ under strict InChIKey. What the paper
claims is the interaction that produces them, which is significant and survives correction; the
reversals themselves illustrate it and are not evidence on their own.

The three additional comparators (BioTransformer, MetaPredictor and MetaTrans) are scored
only on the shared subset of $\nshared$ substrates for which frozen third-party predictions exist,
whereas GRAIL's and SyGMa's headline numbers elsewhere are reported on the full clean test split of
$\ntest$. Within the comparison table itself all five methods are re-scored on the same $\nshared$
substrates, so the columns are paired on identical inputs.

MetaPredictor has since been re-scored on the full test split, at recall@15 of 0.568, close to
SyGMa, which indicates its subset value is representative. BioTransformer and MetaTrans remain on
the subset. A full five-method rerun at a single sample size is future work: BioTransformer's
subset configuration was not scripted, and MetaTrans's inference pipeline no longer reproduces
in-tree.

\subsection{The five-method comparison under four aggregates}

Table~\ref{tab:setmetrics} re-scores every method's deployed output set on the shared subset, with
no top-$k$ truncation, since precision and Jaccard are set quantities. The orderings by recall and
by F1 are different orderings.

\begin{table}[h]
\caption{Macro precision, recall, F1 and Jaccard over each method's deployed output on the shared
subset ($n=\nshared$), under \textsc{inchikey-tautomer}. Rank by recall and rank by F1 in the last
two columns.}
\label{tab:setmetrics}
\begin{center}
\begin{tabular}{lccccc cc}
\toprule
\textbf{method} & prec. & recall & F1 & Jaccard & outputs & rk-R & rk-F1 \\
\midrule
MetaTrans        & 0.122 & 0.565 & \textbf{0.177} & 0.105 & 11.5 & 2 & 1 \\
MetaPredictor    & 0.116 & \textbf{0.585} & 0.174 & 0.101 & 10.9 & 1 & 2 \\
BioTransformer   & 0.127 & 0.453 & 0.162 & 0.104 & 10.7 & 4 & 3 \\
\textbf{GRAIL}   & 0.110 & 0.365 & 0.143 & 0.092 & \phantom{0}8.7 & 5 & 4 \\
SyGMa            & 0.028 & 0.562 & 0.049 & 0.027 & 75.1 & 3 & 5 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Only one method moves with certification, and it is the one the decomposition names. On F1 the four
selecting methods are mutually indistinguishable, every pairwise interval covering zero, the widest
separation being GRAIL against MetaTrans at $-0.033$ $[-0.071,+0.004]$. SyGMa is below all four,
$+0.094$ $[+0.065,+0.124]$ against GRAIL rising to $+0.127$ $[+0.106,+0.149]$ against MetaTrans.
On recall it is above two of them, $-0.109$ $[-0.168,-0.052]$ against BioTransformer and $-0.197$
$[-0.274,-0.120]$ against GRAIL. Those two pairs are the certified reversals: each is significant
in one direction on recall and in the other on F1.

The remaining exchanges of place, MetaTrans past MetaPredictor at the top and BioTransformer past
GRAIL below it, are within noise and we do not claim them. The reading is not that the aggregate
shuffles a leaderboard arbitrarily. It is that the aggregate is decisive for exactly the
architecture the factorisation identifies, the one with no selection stage, and not detectably
different among the architectures that select. That last is a failure to detect rather than a
demonstration of absence: the widest of those intervals still admits a difference of $0.07$ at
$n=\nshared$, and separating the two readings needs a larger shared subset than the comparators
currently supply.

\subsection{External validity rests on 37 substrates}

The external ceiling is measured on 37 GLORYx parent substrates, so its 95\% interval is wide by
construction, at $[0.531,0.733]$. The regression that partially explains the gap between internal
and external coverage is fit on the same small external population. We report it as suggestive
rather than as a law that transfers.

\subsection{One assumption behind Proposition 2 is not measured}

Proposition 2's explanation assumes that the probability of a rule being labelled is proportional
to its marginal firing rate. That propensity model is asserted rather than estimated from data.
The identifiability argument for why a learned selector underperforms a frequency prior is
therefore consistent with the stated mechanism, not proof of it.

\subsection{Proposition 3 bounds single-step application only}

Depth-2 application recovers 0.012 of ceiling at 8.5 times the candidate cost, so the bound
constrains single-step rule application rather than metabolite prediction in general. Multi-step
chemistry and chemistry outside the bank remain open sources of coverage.

\subsection{The factorised generator does not replace the deployed one}

The factorised redesign of the generator is coverage-bound, reaching recall@15 of 0.256 against
the deployed $\grailmacro$ on the same full test split. Its only deployable value is a modest re-ranking gain of $+0.0165$, 95\%
confidence interval $[0.006,0.027]$, on the full clean test. Closing the larger gap to SyGMa still
requires a broader rule bank, which is outside the scope of this paper.

\subsection{The rank-flip result is established within one domain}

The reordering under different matching conventions is demonstrated for metabolite prediction. Whether
matching conventions reorder leaderboards in other molecular-machine-learning tasks, such as
retrosynthesis or molecular generation, is an open question, and the run that would settle it is
specific: re-score the frozen predictions of at least two comparable published models in another
domain under the same five conventions. If the ordering does not move, with an interaction interval
covering zero, the broader claim fails.

That run needs at least two models of comparable strength whose match sensitivity differs and
whose raw ranked predictions are public. Such pairs do not currently exist in a re-scorable form:
the checkpoints that are readily runnable are either from the same family or badly mismatched in
strength. We therefore defer the comparison rather than force it.

Two measurements outside metabolism both come back negative, and together they say something the
paper could not say from one.

The first targets molecular generation, where the matching criterion is not a way of checking an
answer but the metric itself: MOSES reports Unique@$k$ as the count of distinct canonical SMILES
among a model's generations \citep{Polykovskiy_2020}. A model that emits a keto and an enol form of
one compound is credited with two molecules. We re-scored the published generations of all eight
models MOSES ships samples for, 240{,}208 molecules, under the same five criteria.

Tautomer matching is inert on this set. Every one of the eight returns its canonical Unique@10{,}000
to within $0.0001$, the single non-zero difference being JT-VAE's $0.9992$ against $0.9991$. The
relaxation that does something is stereochemistry, and what it does is small: the hidden Markov
baseline falls $0.923 \to 0.888$ and the $n$-gram baseline $0.986 \to 0.957$, while five of the
remaining six move by $0.001$ or less. That is enough for one rank exchange, LatentGAN dropping
$0.998 \to 0.988$ past a combinatorial baseline that does not move from $0.990$, so two adjacent
mid-table models swap positions 5 and 6 under stereo-blind matching and under no other criterion.

We report this as a negative control rather than a null. The ordering is stable under four of five
criteria and the one exchange it admits is between models $0.002$ apart, against a metabolism
result in which a significant advantage between two published tools is erased outright. The reason
is visible in the numbers: seven of the eight sit at $0.986$ or above, so there is little room to
collapse anything, and generators trained on canonical SMILES from ZINC reproduce that canonical
form. The asymmetry is the same one the external metabolite set shows, stereochemistry doing the
work and tautomerism doing none; what differs between the domains is how much is at stake.

The second targets retrosynthesis. Scoring ReactionT5v2 on 200
substrates from the USPTO-50k test set, the same predictions move only about 0.02 in top-1
accuracy across canonical, stereo-stripped, InChIKey and tautomer matching, between 0.695 and
0.715, roughly an order of magnitude less than the interaction we measure in metabolism. A
plausible reason is that retrosynthesis references are canonical, clean small molecules, whereas a
metabolism rule engine emits tautomer and stereochemical variants of its references.

The reading we take from these is not that matching conventions reorder leaderboards everywhere.
Both negatives point at the same precondition, and it is a property of the reference set rather
than of the task: the effect needs references that carry representational variety for a generator
to diverge from. Metabolite references are experimental records, written with whatever tautomer and
stereochemistry the reporting assay produced, while a rule engine emits whatever its templates
produce. ZINC molecules and retrosynthesis targets are canonical by construction, so there is
nothing for a generator to diverge from and the criterion has nothing to change. Whether the convention reorders
results in another domain of comparable tautomer ambiguity, rather than only shifting them,
remains the open test, and it needs only public multi-model predictions in a re-scorable form.

\subsection{The external set is not external to GRAIL}

The criterion ladder of \S\ref{sec:rankflip} is reported on the $\nglory$ GLORYx drugs as an
external replication. Checking two independent curations against each other turned up something
that check was not looking for: $19$ of those $37$ parents are substrates in GRAIL's training split
and $5$ more are in validation. Thirteen are unseen. The overlap costs a minute to compute and had
never been computed.

The contamination is one-sided, which decides how much has to be repaired. SyGMa and BioTransformer
do not train on this corpus, and MetaPredictor was trained by its authors on their own data, so
their figures are unaffected and stay at $n=\nglory$. Only GRAIL's are inflated, and inflated in our
own favour. Restricting the other three to the thirteen would have been an error in the opposite
direction, discarding two thirds of the sample to correct a bias they do not have.

Recomputing on the thirteen with the same loaders, matcher and frozen predictions, so that only the
substrate list changes, GRAIL's external recall falls by up to a factor of $2.6$: $0.155 \to 0.059$
under strict \textsc{inchikey}, $0.310 \to 0.185$ once stereochemistry is disregarded, $0.333 \to
0.223$ under the tautomer-aware default. The other three methods move by less than $0.06$, which is
the sample change rather than a correction.

Two claims survive and one does not. GRAIL's stereo step remains significant at $+0.126$
$[+0.028,+0.239]$ against $+0.155$ on the full set, so the mechanism is real rather than memorised:
GRAIL cannot emit a stereocentre, and a stereo-blind criterion rewards that whether or not it has
seen the drug. Its interaction with SyGMa also survives, at $+0.126$ $[+0.028,+0.239]$. The
interaction between BioTransformer and GRAIL does not: published at $+0.150$ $[+0.069,+0.242]$, it
is $-0.104$ $[-0.223,+0.000]$ on the unseen drugs, reversing sign and losing significance. It was
named one of the two largest and is withdrawn. Of six pairs, four were significant on the full set
and two are on the clean one, both against SyGMa.

The headline external result is unaffected because it involves neither contaminated arm. Under
strict \textsc{inchikey} SyGMa leads MetaPredictor by $-0.130$ $[-0.196,-0.065]$ and one rung later
the advantage is gone; both methods are external to this corpus in the ordinary sense.

We report this rather than quietly recomputing because the failure is instructive about the protocol
the paper argues for. An audit of train-test overlap is cheap, is not implied by any of the other
checks we run, and surfaced here only as a by-product of measuring something else.

\subsection{What two independent curations of the same drug agree on}

Twenty-five of the external drugs are also curated by this paper's corpus, independently, which
makes their two metabolite lists comparable to each other in exactly the way a prediction is
compared to a reference. Nothing in this subsection involves a model.

The criterion moves that agreement by a factor of five. Under canonical SMILES matching the two
curations of the same drug reach a Jaccard of $0.075$; under strict \textsc{inchikey} $0.145$; under
Tanimoto$=$1, $0.239$; stereo-blind, $0.384$; and under the tautomer-aware default $0.406$. One-sided,
the corpus recovers $0.647$ $[0.535,0.756]$ of the GLORYx list and GLORYx $0.539$ $[0.439,0.643]$ of
the corpus list, both under the default. All figures are on the same twenty-five drugs, so none of
this is a population effect. Two experts looking at the same chemistry appear to agree about
one-fourteenth of it under a naive criterion and about two-fifths under a tolerant one; the
difference is notation, not chemistry, and it is the cleanest argument in this paper for declaring
the criterion.

Those two numbers also bound what the benchmark can register, and the bound needs no model. Neither
curation is a predictor --- a source's list is what its inclusion criteria admitted, not an attempt
to reconstruct the other's --- so what the pair fixes is the level at which two independent readings
of the same chemistry coincide: $0.539$ one way, $0.647$ the other. References that no method
retrieves cannot all be counted as model error.

Where models sit against that level is a statement about budget, and the budget is the parameter
\S\ref{sec:metric} declares. Uncapped, scored against the corpus annotation with the same matcher,
MetaPredictor recovers $0.562$ $[0.443,0.676]$, SyGMa $0.524$, BioTransformer $0.480$, GRAIL
$0.338$; MetaPredictor's paired gap to the curation is $-0.023$ $[-0.153,+0.123]$, a tie inside
noise rather than a lead, and it is bought with $10.6$ metabolites per drug against the curation's
$6.0$, SyGMa's near-tie with $95.9$. Capped instead at each drug's own curated set size --- the
budget the curation actually spent, with the curation itself not truncated --- its $0.539$ stands
against MetaPredictor's $0.301$, SyGMa's $0.271$, GRAIL's $0.284$ and BioTransformer's $0.193$, and
every paired gap excludes zero (MetaPredictor $+0.238$ $[+0.123,+0.354]$, the smallest of the four).
A fixed $k{=}6$, the curation's mean output, truncates the curation as well and points the same way
without reaching significance: $0.472$ against $0.394$, gap $+0.078$ $[-0.050,+0.210]$. It is the
more conservative of the two matches, since a curation asserts no ranking and the order its list is
cut in is not one it claims.

Models therefore reach curator-level recall only by spending more output than a curator does. At the
curator's own budget they are behind by a quarter to a third of the reference set.

That per-substrate cap is not an arbitrary handicap, which is the objection it invites. The size
supplied is the curator's own, so it is the per-substrate cardinality an oracle would choose, and
Appendix~\ref{app:props} measures what that oracle is worth and how little of it is obtainable. Both
figures are taken out of sample, from the same cross-fit, so they share a baseline: for
MetaPredictor on the full split the oracle cutoff lifts macro-F1 from $0.254$ at the best constant
$k$ to $0.435$, and a head trained to predict that cutoff recovers $0.008$ of the $0.181$ available,
$4.3\%$ ($3.2\%$ for GRAIL, negative for SyGMa). No method here can set this budget for itself. What
the cap does \emph{not} do is deny the model outputs it could have justified --- under recall a cap
can only cost --- so the comparison reduces to the plain statement: spending what the curator spent,
the curator retrieves $0.539$ of the other source's list and the best model $0.301$.

One reading is not available, and it is the one the framing invites. The curators had the literature
on each of these drugs in front of them; they were recording what had been observed, not predicting
it from structure. So this measures the references, not the contestants: an independent competent
reading of the same chemistry omits $46\%$ of what the corpus records, which is why recall past that
level is increasingly a measurement of one source's inclusion criteria. It is not evidence about
whether models have caught up with expert judgement, and should not be read as any.

Two limits are worth stating rather than leaving to be found. Twenty-five drugs give intervals
around $\pm0.1$, and these are drugs two sources both chose to curate, so they carry $5.7$ to $6.5$
references each against $2.2$ across the corpus. Better-studied compounds are exactly where two
curations have the most opportunity to agree, so $0.539$ is more likely an upper bound on
inter-source agreement than a typical value. Which way that biases the ceiling we cannot say: on
less-studied chemistry both the curations and the models have less to go on, and we have not
measured which of the two degrades faster.
