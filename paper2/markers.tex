% Values the manuscript cannot compute: they come from outside the artifacts and outside the
% code. Each renders as a conspicuous bracket so it cannot survive to submission unnoticed, and
% each is defined in exactly one place so filling it is a one-line edit. This file is shared by
% every journal wrapper, because a marker that lives in a layout preamble goes missing the moment
% the target journal changes -- which is how \ZenodoDoi came to be undefined.
\newcommand{\unresolved}[1]{\textbf{[#1]}}
\newcommand{\ZenodoDoi}{\unresolved{ZENODO DOI, minted on publishing the deposit}}
\newcommand{\CorpusVersions}{ChEMBL~34, DrugBank~5.1.10, MetXBioDB~1.0 and the GLORYx reference set released in 2020, published as \citet{de_Bruyn_Kops_2020}}
\newcommand{\CorpusLicence}{\unresolved{CORPUS LICENCE: whether the derived corpus and the candidate pools may be redistributed, given DrugBank's terms}}
\newcommand{\CuratedProvenance}{\unresolved{CURATED RULES: the published source, if any, of each of the four curated collections, and the licence each carries}}
\newcommand{\CorpusAssembly}{\unresolved{CORPUS ASSEMBLY: the per-source extraction queries and filters, the structure standardisation, the cross-source de-duplication and conflict rule, the per-source counts, and the splitting algorithm and its seed}}
