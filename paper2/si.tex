% Supporting Information, submitted as a separate file as ACS requires.
\documentclass[journal=jcisd8,manuscript=suppinfo,layout=onecolumn]{achemso}

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
% The comparison population is 291 rows and has to break across pages.
\usepackage{longtable}
% Pointers running the other way, from this document into the manuscript, resolve through the
% manuscript's own .aux for the same reason its pointers into this one do: a hand-typed float
% number compiles silently when it is wrong. Both documents are built twice, so each reads the
% other's numbering.
\usepackage{xr}
\externaldocument[MS-]{grail_jcim}
\SectionNumbersOn
\input{numbers}
\input{markers}

\renewcommand{\thepage}{S\arabic{page}}
\renewcommand{\thesection}{S\arabic{section}}
\renewcommand{\thetable}{S\arabic{table}}
% The one equation here is numbered S1 rather than 1, because the manuscript's own
% Equation 1 is the rule this one replaces, and two pages apart the same string named both.
\renewcommand{\theequation}{S\arabic{equation}}
\renewcommand{\thefigure}{S\arabic{figure}}

\author{Nikita L. Polomoshnov}
\affiliation{Faculty of Bioengineering and Bioinformatics, Lomonosov Moscow State University,
Leninskie Gory 1-73, Moscow 119234, Russia}
\email{nikitapol@fbb.msu.ru}

\author{Anastasia V. Rudik}
\affiliation{Department of Bioinformatics, Institute of Biomedical Chemistry,
Pogodinskaya Str. 10-8, Moscow 119121, Russia}
% achemso's suppinfo mode prepends "Supporting Information:" itself, so the title must not
\title{GRAIL: rule-grounded metabolite prediction under a declared evaluation}

\begin{document}

\maketitle

\section{The corpus and its splits}
\label{sec:si-corpus}

\input{si_table_splits}

The annotated substrate--metabolite pairs are assembled from four sources: ChEMBL
\citep{Zdrazil_2024}, DrugBank \citep{Wishart_2018}, MetXBioDB \citep{Djoumbou_Feunang_2019} and
the GLORYx reference set \citep{de_Bruyn_Kops_2020}. The versions used are \CorpusVersions{}.

Their licences do not combine. ChEMBL~34 is released under CC BY-SA 3.0, which requires a
derivative to carry the same or a compatible ShareAlike licence and forbids adding restrictions.
DrugBank~5.1.10 is released under CC BY-NC 4.0, which forbids commercial use. A single derived
work drawing on both cannot satisfy each: adding the non-commercial clause is a restriction
ShareAlike does not permit, and dropping it breaches DrugBank's terms. MetXBioDB~1.0 is
distributed with BioTransformer, whose terms require explicit permission for commercial use or
redistribution.

What the deposit contains follows from that, and the decision is taken here rather than deferred.
The candidate structures are produced by applying this work's rule bank and are not corpus
records, so they are released. Every corpus structure, the substrates as well as the annotated
metabolites, appears only as a tautomer-canonical InChIKey, which is a hash and does not carry
the structure. No source record is redistributed \emph{in the deposit}, and the licence of the
derived corpus is \CorpusLicence{}. The repository is a separate channel and it is settled the same
way, by a substitution rather than by an answer to a question that does not resolve. It ships the
evaluated test substrates as structures, because reproducing the comparison means running a
predictor on one, and their annotated metabolites as the descriptors the matching criteria are
decided by: a hash of the canonical SMILES, the InChIKey, the tautomer key and the on-bits of a
folded Morgan fingerprint. A hash decides string equality exactly and a folded fingerprint decides
Tanimoto of one exactly, so all \numCritCriteria{} declared criteria survive the substitution and
none of the metabolites survives it; a check compares each criterion's verdict on the descriptors
with its verdict on the structures and reports no disagreement. What a reader without the source
licences loses is not a recall figure or a criterion but the structure-level analyses of this
document, which read the reference itself, and those need the corpus that reader already needed.
The deposit's keys must still be joined against a substrate set rebuilt under one's own licences,
and the manifest is what tells them whether their rebuild matches.

\subsection{How the corpus was assembled}

The corpus files were written in a single run months before this repository's first commit, and
no script among its \numAssemblyCommits{} commits reads any of the four sources or writes the
structure files. The assembly cannot therefore be read off code. What follows separates what the
shipped files establish about it from what only their producer could, and states the second as a
bounded absence rather than as silence.

\paragraph{What each record carries.} The three splits hold \numAssemblyRecords{} structure
records, each with five fields: an identifier, an InChI, a state, a SMILES string and an index.
The identifier is the one field that could carry a source accession, and it is empty in every
one of the \numAssemblyRecords{}. Per-source counts are therefore not
recoverable from the data, and none is reported. The state field takes three values, and its
meaning is checked against the triples rather than assumed: every substrate of a positive triple
is marked as a substrate and every product of one is marked as annotated, the remaining records
being rule-applicable products carrying no annotation. The positive-unlabelled negatives this
work trains against are consequently not derived when the data is loaded. They are stored, and
which rule bank produced them is not recorded.

\paragraph{The drawing the standardisation left.} Every structure in the corpus is a fixed point
of the InChI round-trip: \numDialectTrainRtequal{} of \numDialectTrainRtof{} train records,
\numDialectValRtequal{} of \numDialectValRtof{} in validation and \numDialectTestRtequal{} of
\numDialectTestRtof{} in test are exactly what their own stored InChI reads back to, and the
\numDialectTrainRtdifferWord{} exceptions are salts whose counter-ion the SMILES has lost and the
InChI kept. That round-trip places a mobile hydrogen on oxygen, so an amide arrives as the imidic
acid and cytosine as its lactim. This is not the declared standardiser's output: run under
\numDialectRdkitversionsWord{} RDKit releases spanning four years, \texttt{standardize\_mol} returns
the amide in \numDialectRdkitamideWord{} of \numDialectRdkitversionsWord{}, from both the amide and the
imidic acid. The drawing therefore entered before the standardiser this work declares, and rules
are matched against it. Section~\ref{sec:si-presentation} treats it as the axis it is.

\paragraph{De-duplication, and the conflicts there were none of.} There is no de-duplication
code, and with the accession field empty there is no source field a conflict rule could have
arbitrated on. The files show that none was needed. \numAssemblyDuplicated{} substrate
structures appear under more than one index across the three splits, and the two annotation sets
differ in \numAssemblyDisagreementsWord{} of them; \numAssemblyBothwaysWord{}
pair in the whole corpus is listed both as positive and as negative. The effective rule is set
union when the data is loaded, applied to structures after standardisation, with no precedence
between sources because no source is recorded. This is also why the split table counts
\numAssemblyTestids{} test substrates by index and \numAssemblyTeststructures{} by structure.

\paragraph{What is inside, for the sources that can be checked.} Two of the four, MetXBioDB
and the GLORYx reference set, are physically present here and can be intersected with the
corpus by structure skeleton. The corpus holds \numAssemblyCorpuspairs{} distinct annotated
pairs. Of MetXBioDB's \numAssemblyMetxpairs{}, \numAssemblyMetxinside{} are inside it,
\numAssemblyMetxshare{}; of the GLORYx set's \numAssemblyGloryxpairs{} pairs over
\numAssemblyGloryxparents{} parents, \numAssemblyGloryxpairsinside{} pairs and
\numAssemblyGloryxparentsinside{} parents are. Those pairs are counted distinct on the InChIKey
skeleton, which is the key this assembly step uses; Section~\ref{sec:si-typeoverlap} counts the same
set distinct on the typed pair and reaches a larger number, and the two fractions are not
comparable with each other. So the assembly ingested neither source
wholesale: a filter of unknown shape dropped nearly two fifths of MetXBioDB, and the measurement
bounds that filter without recovering it. What it does not do is attribute any pair to a source,
because a pair in MetXBioDB may equally have entered through DrugBank. ChEMBL and DrugBank are
not present here and admit no such measurement, so no fraction is claimed for them.

\paragraph{The partition, and its seed.} The three splits are substrate-disjoint and not
molecule-disjoint, which the leakage audit records and the main text states. The repair that
produced the shipped clean triples ran in the substrate-level mode of
\texttt{scripts/fix\_splits.py}; its molecule-disjoint mode is a different partition and was not
used. Test was passed through untouched, and its clean file is byte-identical to its input. The
three shipped clean files match the digests the split manifest declares. There is no split seed:
the repair is deterministic and draws on no random number generator, and the partition it repairs
has no producer in this repository. It is a fixed historical artifact carried forward by digest.

\paragraph{What a third party can and cannot rebuild.} With their own licences for the four
sources, every source record can be obtained. What cannot be reconstructed is the extraction and
filtering that selected from them, because that step is not in the repository and the data
carries no accession to reverse it by. They can, given the structure files, reproduce every step
from there: the standardisation is declared, the label convention is checked above, the repair is
deterministic and released, and the manifest's digests confirm a rebuild rather than merely
report that one differs. The unrecoverable step is the first one, and it is the whole of what is
missing.

The annotated metabolites of the evaluated test set are released as matching descriptors, in
\texttt{results/test\_reference\_descriptors.json}, alongside every arm's per-substrate
predictions. All \numCritCriteria{} matching criteria are therefore recomputable on the comparison
population, not only the default one, and none of the metabolites is. That is checked rather than
asserted: \texttt{reproduce\_from\_descriptors.py} rebuilds the whole comparison under all
\numCritCriteria{} from the released descriptors and the released predictions, reading no corpus
structure, and its default-criterion column for the whole bank matches Table~\ref{MS-tab:sweep} at
every budget. The repository and the Zenodo deposit differ in this, and the repository is the wider
of the two.

The corpus is divided by substrate, so no substrate of one split appears in another. Molecular overlap between splits is non-zero and expected, because a molecule
that is a substrate in one split may appear as a product in another: \numLeakMolOverlap{}
molecules occur in both train and test, and \numLeakSubAsMol{} test substrates appear as molecules
in train. Both counts are recorded so that neither is later discovered and mistaken for leakage.

Three evaluation populations are drawn from these splits, and every figure in the paper names
one. The evaluated test set is the \numPopulationTestsubs{} test substrates that carry at least
one reference, holding all \numPopulationTestrefs{} references of the split; the substrates it
excludes, \numSplitTestSubstrates{} minus \numPopulationTestsubs{} of them, carry none. The
validation draw is a sample of \numValdrawCap{} substrates at seed \numValdrawSeed{}, of which
\numValdrawDeclared{} produced a candidate pool and \numValdrawPaired{} are held by both arms of a
paired comparison. The comparison set is the \numPopulationN{} substrates on which every method
predicts.

No external evaluation set is reported. GLORYx is one of the four sources this corpus is
assembled from, so its \numGloryxN{} parent substrates are not external to it, and the
\numGloryxOverlap{} that fall inside train or validation are there by construction rather than by
leakage. Holding out one source would not produce an external set either, because the sources
overlap one another: MetXBioDB and the GLORYx reference set both draw on the same published
metabolism, and DrugBank overlaps both.

The same fact bounds the comparison. Rule-based comparators have no training set that could be
held out, and their rules may encode the metabolism reported in these sources: BioTransformer is
built on MetXBioDB, and GLORYx was developed against the reference set of that name. Their
knowledge overlaps our test substrates by construction. No prior benchmark in this area controls
this asymmetry, and neither does this work; it is reported because anyone comparing these numbers to
published ones should know that the rule-based figures are, in this respect, optimistic.

\section{What the annotation contains}

Every recall figure in this paper is a statement about this annotation, and the paper reported the
figures at length without describing it. The census is here. Nothing in it is a model output; it
is the corpus read back.

The evaluated test set carries \numRefauditReferences{} annotated metabolites over
\numRefauditSubstrates{} substrates, \numRefauditDistinct{} of them distinct under the paper's
default key. All \numRefauditReferences{} parse and all are keyable, so no reference is dropped
before scoring. \numRefauditShared{} keys are shared by more than one substrate, which is expected:
a common metabolite of two related drugs is one molecule.

By heavy-atom count against the parent, \numRefauditSmaller{} references are smaller,
\numRefauditSame{} are the same size and \numRefauditLarger{} are larger. The median change is
\numRefauditDeltamedian{} heavy atoms and the fifth and ninety-fifth percentiles are
$\numRefauditDeltalo$ and \numRefauditDeltahi{}, but the tails are long in both directions: the
extremes are $\numRefauditDeltamin$ and $+\numRefauditDeltamax{}$. A bank of single-template edits
is not the natural instrument for either tail, and the coverage census in the main text is where
that shows.

Ten elements appear. Carbon, oxygen and nitrogen account for almost all of the atoms, with
sulfur, chlorine, fluorine, phosphorus and bromine behind them and iodine and selenium in single
figures. Nothing in the annotation is outside what the rule bank's chemistry addresses.

Two properties bear directly on how the recall figures should be read.
\numRefauditSelfref{} references are equal to their own substrate under the default key, an
annotation that names a molecule as its own metabolite, and the parent-drop convention
discards exactly those, which is why that convention's effect is negative for the substrates
concerned rather than zero. And the annotation is positive-unlabelled throughout: a pair that
does not appear is not a pair that was tested and rejected. That is why precision is reported and
is not used to order systems, and why an absent prediction is never counted as a false positive
anywhere in this paper.

\section{The matching criteria}

\input{si_table_criteria}

What counts as a hit is an axis with five declared settings, given in Table~\ref{tab:si-criteria}.
The paper reports its comparison under the tautomer-aware key and its sensitivity to the other
four in Section~\ref{sec:si-sensitivity}.

\section{The parent-drop convention}

\input{si_table_parentdrop}

Table~\ref{MS-tab:sweep} declares that a prediction equal to the substrate is dropped before the budget, for every
method alike. Most other declared choices here are swept, and this one was declared and
not measured, so Table~\ref{tab:si-parentdrop} measures it. The pool cap is the one that is
not: it is fixed by a rule rather than by a search, and what that rule costs is measured against
the alternative of not capping at all rather than across a range of caps.

The arms behave very differently, which is what makes this a strong test rather than a formality.
SyGMa never returns the substrate, in none of \numPopulationN{} lists, while
\numPdropReturnsBank{} exhaustive-mode lists contain it at a median rank of
\numPdropMedrankBank{}. A convention applied uniformly to arms that differ that far in how often
it bites has the maximum opportunity to move one and not the others, so a null result here is
informative and not merely the absence of an effect. It does not move them: the
largest effect anywhere in the grid is \numPdropMaxEffect{}, no cell of
\numPdropCells{} separates from zero, and the effect is at most \numPdropShareofmarginPct{} of the narrowest
separating margin anywhere in the comparison, which is \numPdropNarrowestmargin{}. Where it is negative, the cause is the \numPdropSelfref{} substrates of this population whose
own tautomer key is among their annotated references, for which the convention discards a hit the
annotation counts. Across the whole evaluated test set there are \numRefauditSelfref{} such
substrates; the comparison set holds \numPdropSelfref{} of them.

\section{What kind of molecules these are}
\label{sec:si-applicability}

The corpus is assembled from four drug-centric sources and the manuscript motivates the problem
with environmental chemicals as well, so what the substrates actually are belongs on the record
beside the annotation, which is characterised elsewhere by heavy-atom change and element
composition. Table~\ref{tab:si-applicability} describes them.

They are drug-like. Median molecular weight is \numAdomainEvalMw{} on the evaluated test set with
a calculated logP of \numAdomainEvalLogp{}, and the comparison set sits in the same place, which
is one more reading on whether that draw is unlike the split it came from. The
\numPopulationTestsubs{} substrates carry \numAdomainEvalScaffolds{} Bemis--Murcko scaffolds and
\numAdomainEvalSingletons{} of those are carried by a single substrate, so the population is
diverse in skeleton rather than a few series sampled repeatedly.

What that does not establish is an applicability domain. A domain is a statement about where
performance holds, and it would need recall measured against these axes rather than the axes
described; the table says what was measured on, and a reader whose compounds sit outside this
range should read every figure here as untested for them.

\input{si_table_applicability}

The members are Table~\ref{tab:si-population-list}, under the key every comparison here is
scored by. How those \numPopulationN{} were drawn from the split is not recorded anywhere and
that is stated where the population is defined; naming them at least makes the draw checkable
against anything a reader wants to check it against.

\input{si_table_population_list}

\section{Every candidate count, and what each one is}
\label{sec:si-counts}

Several counts of how many candidates a mode returns appear in this work, and they differ because
they are measured at different stages and on different populations rather than because any of them
estimates the others. They are collected here so that a reader comparing two of them can see at
once which is which.

\input{si_table_counts}

The pool is what rule application produced before duplicates are removed; the list is what a caller
receives, deduplicated by matching key and capped. The emission rule reads the pool at its own
stage, before the cap. The worked example is one substrate and carries no population at all.

\section{Where a method runs out of candidates}

\input{si_table_short}

At the widest budgets the comparison is partly measuring list length rather than ranking, and the
counts that say by how much are here at every budget rather than only at the one the main text
prints. They bear directly on the two leads claimed at $k=30$ and $k=50$: an arm that has run out
of candidates cannot gain from a wider window, so the leads should be weighed against the
number of substrates for which that is the case.

\section{Which chemistry each method recovers}
\label{sec:si-chemistry}

A recall figure aggregates over transformations of very different kinds, and a reader deciding
whether any of these systems is usable on their compound class cannot read that off one number.
Table~\ref{tab:si-chemistry} splits the comparison set's \numChemReferences{} references into
\numChemClasses{} classes and reports recall within each.

A class here is a change in molecular formula. That is what the annotation determines: the corpus
records a substrate and a product, not a mechanism, so a mechanism cannot be recovered from it
without guessing. One added oxygen is aliphatic hydroxylation, aromatic hydroxylation,
N-oxidation or S-oxidation, and nothing in the record separates them; the class is named for the
change and the ambiguity is stated with it in the artifact. Of the references, \numChemUnresolvedWord{} failed to resolve to a
structure.

Three things in the table are worth naming. The largest class is not a functionalisation at all
but a cleavage losing two or more carbons, \numChemCleavageRefs{} references, and it is where the
whole bank pays for its size: \numChemCleavageBank{} against \numChemCleavageBestother{} for the
best comparator. The second largest, \numChemOtherRefs{} references, is the class of deltas this
vocabulary does not name, and no method exceeds \numChemOtherBest{} on it, so a fifth of the
annotated chemistry is transformation types none of these systems is built for. And of the
\numChemDiolRefsWord{} references that add two oxygens, five of the six methods recover none and
the sixth recovers \numChemDiolBest{}: a shared blind spot rather than a difference between
systems.

Two more are worth naming because they run the other way. The exhaustive arm is not the best arm
on \numChemBehind{} of the \numChemClasses{} classes, and on
\numChemIntahead{} of them it is below the interactive arm, which returns a sixth as many
candidates. Demethylation is the clearest: \numChemDemethbank{} exhaustive against
\numChemDemethinter{} interactive and \numChemDemethbest{} for the best comparator, on
\numChemDemethrefs{} references. A wider pool cannot lose coverage, so what is lost there is
position: candidates the smaller rule budget never produced are ranked above the correct answer
and push it past fifteen. That is the same mechanism the ranking ablation measures in aggregate,
seen on the class where it costs most.

Recall within a class is over that class's references alone, so the small classes carry wide
uncertainty. No intervals are quoted for them and none should be read into the ordering of a row
with five references in it.

\input{si_table_chemistry}

\section{The comparison at matched list length}
\label{sec:si-matched}

A nominal budget is not a length. At $k = 50$ most comparator lists have already ended while the
exhaustive arm is still at its cap, so declaring the budget does not control the thing the budget
was introduced to control, and a difference read there is partly a difference in how much each
system emits. Table~\ref{tab:si-short} gives the exhaustion rates; this gives the comparison
with the length removed as a variable.

The design is deliberately hostile to this work. On each substrate both arms are cut to the number
of candidates that comparator returned on that substrate: where MetaPredictor returns two, the
exhaustive arm is allowed two. The two are then read over the same slots on every substrate rather
than on average, which is what a nominal budget cannot do.

One bound on that length is this work's and has to be declared, because a slot count is a
configuration and this paper's argument is that a configuration left unstated is the defect. The
comparator's own list is deduplicated, has a prediction equal to the substrate dropped, and is
then cut at \numMatchedCap{} candidates, five past the pool cap of \numHNineCap{}: beyond that
neither of this work's arms has a candidate to put in the slot, so the comparison there would
measure the pool cap rather than the ordering. The cut binds on \numMatchedBindssygma{} of the
\numPopulationN{} substrates for SyGMa and \numMatchedBindsmetatox{} for MetaTox, and on none
for either of the two arms whose lists are shorter than the cut everywhere, MetaPredictor and
BioTransformer. What it costs the comparators is measured rather than assumed: every contrast
in Table~\ref{MS-tab:si-matched} is identical with the cut removed, to four decimal places, because
SyGMa places \numMatchedPastcutsygmaWord{} of the annotated metabolites past that rank, on any
substrate where its list runs longer.


The result splits the wide-budget leads in two. Against SyGMa the exhaustive arm keeps
$+\numMatchedBanksygma$ $[+\numMatchedBanksygmaLo, +\numMatchedBanksygmaHi]$ over
\numMatchedBanksygmaSlots{} slots, which is an ordering result and the largest margin that
survives this control. Larger differences are printed elsewhere in these documents, at nominal
budgets where list length is not held; those are the ones this control exists to discount, so the
comparison is between the margins in this table and not with them. Against the other three nothing survives:
$+\numMatchedBankmetatox$ $[\numMatchedBankmetatoxLo, +\numMatchedBankmetatoxHi]$ against
MetaTox, $\numMatchedBankmetapredictor$
$[\numMatchedBankmetapredictorLo, +\numMatchedBankmetapredictorHi]$ against MetaPredictor and
$\numMatchedBankbiotransformer$ $[\numMatchedBankbiotransformerLo,
+\numMatchedBankbiotransformerHi]$ against BioTransformer, every interval covering zero and two
of the three pointing the other way. What the sweep reports as a lead over those three at wide
budgets is therefore a statement about list length, and this work's own thesis requires saying so.
BioTransformer's row reproduces the figure its own section reports from a different instrument,
which is the check that the two agree rather than a second reading of the same number.

The interactive arm loses to MetaTox at matched length by
$\numMatchedTrainedmetatox$
$[\numMatchedTrainedmetatoxLo, \numMatchedTrainedmetatoxHi]$, which is the same deficit the main
text reports at fixed budgets and is not an artefact of either.

\section{The comparison under the other aggregation}
\label{sec:si-macro}

Every figure in the main text's comparison is micro, the ratio of sums. Macro, the mean of
per-substrate recall, weights a substrate carrying one annotated metabolite the same as one
carrying twelve, and the two therefore answer different questions rather than estimating each
other. Which one is used is a choice of the same kind as the matching criterion and the output
budget, so the whole comparison is recomputed under it.

\input{si_table_macro}

The choice moves eight verdicts of the seventy-two contrasts computed under both, and the
table's caption names each. Three of them are cells the main text states: the exhaustive
arm's lead over MetaPredictor at a budget of fifteen and BioTransformer's trail at a budget of
one, both of which separate under micro and not under macro, and the interactive arm's trail
against MetaTox at twenty, which does the same. The wide budgets the paper reads its leads at
are unaffected. It also moves the levels, upward for every arm but one,
because substrates with few references are easier and count for more under this aggregation;
BioTransformer is the exception and its macro recall is the lower of the two at the widest
budgets. A
reader who suspects the aggregation was chosen to suit the result can read which way each moved
cell goes from the caption, and the one that bears on a claim goes against this work.

\section{Precision}

Precision is reported because a reader is entitled to it and not because it decides anything here.
Precision at $k$ is hits over the predictions inside the window, so an arm whose list is shorter
than the budget is not charged for the empty slots; that is why MetaPredictor is flat from $k=15$
onwards, having emitted its whole list by then.

These figures order the systems differently from recall, and the ordering is not used. The
annotation is incomplete, so a predicted metabolite that is real but unannotated is counted as a
false positive. Every value below is therefore pessimistic by an unknown amount, and the amount is
larger for an arm that emits more, which flatters the short lists. Recall carries the claims in
this paper for that reason.

\input{si_table_precision}

\section{The comparison's differences and their intervals}

\input{si_table_intervals}

The main text reports the level of each arm and the verdict at each budget. Table~\ref{tab:si-intervals}
gives the quantity those verdicts are read from: the difference between each GRAIL arm and each
comparator, with the paired interval. Two of them are worth seeing rather than inferring. The
exhaustive mode's lead over MetaTox at $k=30$ excludes zero by little, and its two unresolved
budgets, $k=15$ and $k=20$, have intervals straddling zero narrowly rather than lying far from
it.

\section{Sensitivity of the comparison to the matching criterion}
\label{sec:si-sensitivity}

The verdict grid itself is Table~\ref{MS-tab:criterion} of the manuscript, since it is the
evidence the paper's title advertises and belongs where the argument is made. The levels it is
read from are Table~\ref{tab:si-criterion-levels}: every arm at every budget under every
criterion, which is what lets a reader see whether a sign moved because a level moved or because
the arms reordered. What follows is what the grid does not show on its face.

\input{si_table_criterion_levels}

The paper argues that a recall figure depends on evaluation choices that this literature leaves
unstated. The argument is made about published numbers, and Table~\ref{MS-tab:criterion} applies
it to our own: the same comparison, the same population, the same ranking, the same parent-drop
convention, with only the criterion moving.

Ranking is settled before any key is taken, so the criterion changes what counts as a hit and
never what is ranked. The parent-drop convention is re-derived under each criterion so that it
means the same thing in every column.

The two criteria under which GRAIL never leads sit at opposite ends of strictness. Canonical
SMILES equality is the strictest of the five, and under it a hit requires that two pipelines
produce the same string for the same molecule, which is a property of their standardisation
conventions rather than of their chemistry. Tanimoto similarity of one on Morgan fingerprints is
the loosest, and it merges structures a fingerprint cannot separate, which credits every method
with hits it did not earn and credits the comparators with more of them. Neither is a defensible
default, and the paper's argument is not that one criterion is right but that the choice has to be
declared and its consequences shown.

The strict end carries a second entanglement that a reader should have before the column is read
as strictness alone. The references are stored in the corpus's drawing, which writes amides as
imidic acids (Section~\ref{sec:si-presentation}), and every method's output passes through a
standardiser that writes the amide. Canonical SMILES equality between the two is therefore partly
a test of whether a method reproduces the corpus's own tautomer rather than of whether it found
the molecule. What that does not explain is the shape of the column, because the loosest
criterion produces the same one: under Tanimoto similarity of one the verdicts are identical to
canonical's at every budget, and those are the two criteria under which no advantage of this
work separates anywhere. Whatever the two ends share, it is not strictness and it is not the
dialect, and the entanglement is stated here as something a reader of the strict column should
know rather than as the reason it reads as it does. The tautomer-aware key exists because
reproducing a corpus's tautomer is not the question a comparison should be asking, and the
canonical column is reported because declaring a criterion means printing what it does, not only
the one whose behaviour suits the argument.

The reference row of Table~\ref{MS-tab:criterion} reproduces the verdicts of the main text's
comparison exactly, though it is computed by a different script from the same frozen predictions.
The number of references differs by one between criteria, at \numPopulationReferences{} for three
of them and one fewer for two, and the two cases have different causes. Under the first block of
the InChIKey, the thiol and thiolate annotated for butyrylthiocholine merge, because that block is
the skeleton hash and carries neither the protonation layer nor stereochemistry; the full InChIKey
separates them in its final character. Under Tanimoto similarity of one, two of the carboxychromanol
metabolites of $\alpha$-tocopherol merge, because they differ by a single isoprenoid unit in the
side chain and a Morgan fingerprint of fixed radius counts the substructures a repeating unit
contains without counting how many times it repeats. Both
merges are the criterion acting as declared rather than a defect, and both are the reason the
count is reported per criterion rather than once.

\section{How the substrate is drawn}
\label{sec:si-presentation}

This paper sweeps five ways of judging whether a prediction is a hit and nine ways of deciding how
many predictions to make. It did not say how the substrate is drawn. That is a third choice of the
same kind, it is not neutral, and it decides which rules can fire at all.

\paragraph{The corpus speaks one dialect.} Every structure in the corpus is a fixed point of the
InChI round-trip, and the round-trip places a mobile hydrogen on oxygen. An amide is therefore
stored as the imidic acid, $\mathrm{R\text{-}C(=O)\text{-}NH\text{-}R'}$ arriving as
$\mathrm{R\text{-}C(OH)=N\text{-}R'}$, and cytosine as its 4-imino-2-hydroxy lactim. Across the
evaluated test set, \numDialectEvalMoved{} of \numDialectEvalN{} substrates are not fixed points
of the declared standardiser and \numDialectEvalImidic{} carry an imidic-drawn amide. The same
holds split by split, counted over the distinct structures each split marks as a substrate rather
than over the substrates that carry a clean triple, which is the population
Table~\ref{tab:si-splits} counts: \numDialectTrainMoved{} of \numDialectTrainN{} in train,
\numDialectValMoved{} of \numDialectValN{} in validation, \numDialectTestMoved{} of
\numDialectTestN{} in test. The two denominators differ for the reason just given and by the
amount it predicts: test marks \numDialectTestN{} structures as substrates and
Table~\ref{tab:si-splits} counts \numAssemblyTeststructures{}, the
\numAssemblyTestmarkednotripleWord{} that differ carrying no clean triple. Every one of the \numDialectEvalMoved{} moves is a pure tautomer move,
invisible to this paper's default matching criterion: the drawing changes which rules fire and
does not change what molecule it is.

\paragraph{The bank learned that dialect.} Mining reads these pairs, so the templates inherit the
drawing. Counting templates by what their reactant side requires, the mined templates ask for an
imidic reactant \numDialectBankMinedimidic{} times against \numDialectBankMinedamide{} for a real
amide: in the portion of the bank this work built, the unpopulated tautomer of the amide is the more
frequently required of the two. The curated collections, written by chemists, run the other way,
\numDialectBankCuratedamide{} amide against \numDialectBankCuratedimidic{} imidic, and not one of
SyGMa's \numSygmaRules{} templates requires the imidic form. Amide hydrolysis and amide
$N$-dealkylation are core phase-I chemistry, so this is not a corner of the bank.

\paragraph{What it does to the worked example.} Gemcitabine is stored as the lactim. On that
drawing the interactive mode finds \numCaseInterFound{} of \numCaseReferences{} annotated
metabolites, and the one it finds is produced by mined template \numCaseInterRulehit{}, whose
reactant side requires the exocyclic imine and which cannot fire on the drug as a chemist draws
it. Presented with the 4-amino-2-oxo structure instead, the interactive mode finds
\numCaseInterdrawnFound{} of \numCaseReferences{}. The exhaustive mode still finds all
\numCaseExhdrawnFound{}, at ranks \numCaseExhdrawnRanks{} against \numCaseExhRanks{} on the stored
drawing, and the deamination it had attributed to a mined template is found instead by curated
template \numCaseExhdrawnDeamrule{} at rank \numCaseExhdrawnDeamrank{}. Both drawings are reported
in Section~\ref{sec:si-case} for that reason.

\paragraph{The arms did not all meet the same drawing.} Five of the six arms of the comparison
were handed the substrate exactly as the corpus stores it: both modes of this system, and SyGMa,
MetaPredictor and BioTransformer. MetaTox was not. Its submission file re-tautomerises the substrate to the
natural form for \numDialectMetatoxretaut{} of the \numDialectMetatoxn{}, on the stated ground that sending an unnatural
imidic acid to an external service would be unfair to that service. The reasoning is right and the
asymmetry it leaves is real, and it runs against the arm that took the trouble: MetaTox met the
drawing its rules expect and SyGMa, whose published templates are written entirely in the amide
notation, did not.

\paragraph{What it does to the comparison.} The axis is swept like the others. Both GRAIL arms
were rebuilt on the identical substrates, presented as the declared standardiser draws them, with
the same checkpoints, the same rule budgets, the same pool cap and the same ranking; the pools
stay keyed by the corpus string, so the annotation is one annotation and the two drawings are
paired substrate by substrate. Table~\ref{tab:si-dialect} gives the paired effect on each arm at
each budget. The largest anywhere is $\numDsweepExhLargest$ for the exhaustive mode and
$\numDsweepInterLargest$ for the interactive one; \numDsweepExhSeparatingWord{} of
\numDsweepExhBudgetsWord{} cells separate from zero for the first and
\numDsweepInterSeparatingWord{} of \numDsweepInterBudgetsWord{} for the second.

Two properties of that measurement should be stated before it is read. The stored column
reproduces the comparison table of the main text at every one of its cells, which is the check
that the sweep measures the same quantity and not a near neighbour of it; and pointing both
dialects at the same pools returns exactly zero everywhere with every interval collapsed, which is
the check that the pairing is right. A sweep that could not show a non-difference as zero would
hide its own defect inside a plausible number.

Carried to the verdicts, the drawing moves \numDsweepMoved{} of \numDsweepCells{}
arm-against-comparator cells, and it is not a lead the paper claims but a trail: the interactive
arm against MetaPredictor at a budget of three, which separates under the standardiser's drawing
and does not under the corpus's. The exhaustive mode's
leads at $k=30$ and $k=50$ and SyGMa's at $k \le 5$ stand under both drawings. The coverage
ceiling on this population is \numDsweepCeilingstored{} under the stored drawing and
\numDsweepCeilingdrawn{} under the standardiser's, a difference of $\numDsweepCeilingdiff$,
$95\%$ CI $[\numDsweepCeilinglo, \numDsweepCeilinghi]$, which does not separate from zero. It is
measured here rather than on the full evaluated split because the uncapped exhaustive pool exists
in both drawings for this population and not for that one.

Only this system's own arms are swept in that table: a difference against a comparator held at
one drawing measures how much of our own margin was the drawing, which is the question that
matters for reading the comparison. What it leaves is a residual on the comparators' side, and
three of the four can now be re-run, so that residual is measured rather than named. Giving SyGMa
the natural drawing raises its recall at every wide budget, by $+\numSygdialectThirty$
$[\numSygdialectThirtyLo, \numSygdialectThirtyHi]$ at $k=30$. Only MetaTox cannot be re-run, and
it is the one comparator that was handed the natural drawing to begin with.

\input{si_table_dialect}

Each of those measurements holds the other arms fixed, and none of them answers the question a
reader of the comparison table has: what the comparison reads when everything that can move is on
one drawing. Table~\ref{tab:si-equalised} is that reading, and it moves \numEqualisedMovedWord{}
of the nine verdicts. The levels move, downward for this work's arms and upward for the two
comparators that gain from the drawing, and they move together: the three-way division of the
comparison is a property of the systems and not of the dialect it was read in. Both of this work's arms are rebuilt on
the standardiser's output, MetaPredictor and BioTransformer are scored from their re-runs on the
natural drawing, and SyGMa is re-run here; MetaTox cannot be moved and is carried unchanged and
marked, since it is also the one arm that received the natural drawing to begin with.

\input{si_table_equalised}

MetaTox is the strongest comparator at the budgets this work reads its leads at, and it is the one
arm the table cannot move, so the second verdict row is read against an arm still on its own
input. The third sets it aside, and there the exhaustive arm leads at
\numEqualisedDifferswithoutmetatoxWord{} budget more. What that row says is not that the lead is
larger but that the comparison among the five arms which could all be put on one drawing is
cleaner than the comparison including the one that could not.

SyGMa is the one comparator that can be re-run, so what that cost it is measured rather than
supposed. Given the substrate as the declared standardiser draws it, on the same \numSygdialN{}
substrates and against the same annotation, its micro recall rises at every budget above the
first: by $+\numSygdialDiffOneFive$ at $k=15$, $95\%$ CI
$[+\numSygdialLoOneFive, +\numSygdialHiOneFive]$, $+\numSygdialDiffThreeZero$ at $k=30$ and
$+\numSygdialDiffFiveZero$ at $k=50$. \numSygdialSeparating{} of the \numSygdialBudgets{}
intervals exclude zero, and they are the four widest budgets, which are the budgets at which
the main text claims a lead over SyGMa. The corpus's drawing was costing the comparator recall,
and the amount is now on the record. It does not reverse the comparison: the exhaustive mode's
lead over SyGMa at $k=30$ is several times this correction. It does mean the lead is over a SyGMa
handed a drawing its rules were not written for, and this amount should be subtracted before reading
it.

MetaPredictor is a source checkout with its weights on disk, so the same correction is measured
for it. That matters because it is one of the two comparators the wide-budget lead does not survive
against, so a correction it did not receive would be the one correction running this work's way.
It is measured on the \numMpdrawRerun{} substrates whose two drawings are different molecules, the
rest keeping their frozen predictions because they contribute a paired difference of exactly
zero. Which substrates those are is asked of the standardiser, not of the submission file: an
earlier pass compared the two columns of the map prepared for the incumbent service and scored
\numMpdrawRerunOld{} of them, missing two whose drawings differ only in how a charge is written, a
sulfoxide against its charge-separated form and a phenol against its phenolate. A string test does
not see those and a molecule test does. Including them lowers the conditional correction rather
than raising it, which is the direction that matters here, since this is the one correction that
would otherwise run this work's way. The natural drawing raises MetaPredictor's recall by
$+\numMpdrawAllDiffThreeZero$ $[\numMpdrawAllLoThreeZero, +\numMpdrawAllHiThreeZero]$ at $k=30$
over the whole comparison set, an interval covering zero, and by
$+\numMpdrawMovedDiffThreeZero$ $[\numMpdrawMovedLoThreeZero, +\numMpdrawMovedHiThreeZero]$
over the \numMpdrawMovedN{} substrates it affects.

BioTransformer, the fourth, is the case that shows why a proxy would have been the wrong
instrument. The natural drawing changes its recall by $\numBtarmDrawingdelta$ at $k=30$, in the
opposite direction: what costs SyGMa recall costs it nothing, which is what a tool that
standardises its own input before matching would do. Three rule-based comparators, three
different answers, and no one of them bounds another.

\section{The rule bank}
\label{sec:si-bank}

The bank holds \numBankRules{} templates spanning \numBankTypes{} distinct reaction types, of
which \numBankParses{} parse under RDKit 2022.09.5. It is the deduplicated union of three curated
collections, a fourth that was carried as curated and is a second machine extraction, and the
templates mined from the training split alone, which is disjoint by substrate and not by molecule.

The two halves are counted rather than described, and the split between them turns out to be
drawn in the wrong place. \numCuratedTotal{} templates are nominally curated and
\numMinedRules{} mined. Three curated collections ship with the code:
\texttt{data/smirks.txt} (\numCuratedHydroxylation{}), \texttt{data/merged\_smirks.txt}
(\numCuratedMerged{}) and \texttt{resources/notebooks\_rules.txt}
(\numCuratedNotebooks{}), contributing \numAttribNamed{} distinct rules to the bank after
deduplication. The remaining \numAttribUnattributed{} match none of those three files.

They are not curated, and three independent marks say so. \numAttribPlaced{} of the
\numAttribUnattributed{} are verbatim lines of a file whose one commit in the history describes
it as extracted reaction rules. They are written in element notation with multi-component
reactant groups, like that file and unlike every hand-written collection here. And they carry the
corpus's imidic drawing at the rate the mined templates do, \numAttribUnattributedimidic{}
against \numAttribMinedimidic{}, where the named expert collections carry it at
\numAttribNamedimidic{}, a factor of thirty-seven; that rate is the fingerprint of a template
extracted from these annotated pairs rather than written by a chemist.
The bank therefore holds two machine extractions and one curated body, in two different template
notations, and the curated body is \numAttribNamed{} templates rather than \numCuratedTotal{}.
Where the named collections came from can be settled for most of them by measurement rather than
recollection, and the answer is sharper than the curated half's own total suggests. All
\numThirdpartyInnamed{} of the borrowed templates fall inside the named body of
\numThirdpartyNamed{} and \numThirdpartyInextractionWord{} among the
\numAttribUnattributed{} of the earlier extraction, so the share to read is
\numThirdpartyNamedsharePct{} of the collections described as curated chemistry rather than
\numThirdpartyTraceablesharePct{} of the half they sit in. They are present character for
character in a rule set somebody else published: \numThirdpartyBiotransformerShipped{} of BioTransformer's
\numThirdpartyBiotransformerRead{} reaction templates; \numThirdpartySygmaShipped{} templates
attributed to SyGMa, which is more than the \numSygmaInside{} of its own distribution counted below
because GLORYx's rule file carries a further set of SyGMa rules in a notation that distribution does
not use; and \numThirdpartyGloryxShipped{} that GLORYx's file attributes to itself. String equality
cannot trace a rule that was rewritten, so this is a lower bound on borrowing and never an upper
one, and the \numThirdpartyUntraceable{} templates matching nothing are not thereby shown to be
original.

The curated half is therefore largely not original to this work, and saying so is the point of
measuring it. It also carries an obligation: these rule sets are distributed under terms of their
own, which a bank containing them verbatim inherits, and which differ between the
\numThirdpartyHoldersWord{} rightsholders involved. The repository additionally tracks
\numThirdpartyFilesWord{} of their files verbatim, \numThirdpartyFilesunusedWord{} of which
contribute no template the bank uses.

The terms, per rightsholder, are these. BioTransformer's distribution is LGPL, and its own README
additionally requires explicit permission for commercial use or redistribution; that permission
has not been sought, and the released model is intended to run as a service, so it is an open
obligation to its authors and not a settled one. SyGMa's installed distribution records
\texttt{License: GPL} with no version and ships no licence text, which is read here the way a
version-less GPL offer is conventionally read, as any version at the recipient's choice; if the
upstream repository names a version, that version governs and this reading is wrong. GLORYx's rule
file carries no licence text this repository holds. Of the four rule files taken from other
projects, \numThirdpartyFilesunusedWord{} are no longer tracked here, including one under a
NonCommercial ShareAlike licence that no GPL release can carry; the templates themselves remain in
the bank and the obligations above run with them. Neither of the first two is settleable here, so the release does not carry the templates they
run with: the \numRelbankRemoved{} present verbatim in BioTransformer's published set are removed
and \numRelbankReleased{} of \numRelbankMeasured{} ship. The removal costs nothing, which is the
next paragraph's measurement. The repository is released under \RepoLicence{}, which is what the
remaining GPL templates permit: a permissive release was never available, and this is recorded as a
consequence rather than a preference.
\texttt{NOTICE.md} carries the same decisions in full, with the file-by-file table.

What removal costs is measurable in the instrument that found the borrowing, and it separates the
obligations sharply. Applying each variant of the bank on the same substrates under the same
conventions, dropping every borrowed template loses \numLrcAllLost{} of \numLrcReferences{}
references, $\numLrcAllChange$ $[\numLrcAllLo, \numLrcAllHi]$. None of that loss is
BioTransformer's, whose \numThirdpartyBiotransformerShipped{} templates are the largest borrowing
here and cost \numLrcBtLostWord{} of them: every reference they reach is reached by something
else in the bank. The loss divides between the other two, \numLrcSygmaLost{} references for SyGMa's
\numLrcSygmaDropped{} templates and \numLrcGloryxLost{} for GLORYx's \numLrcGloryxDropped{},
$\numLrcSygmaChange$ $[\numLrcSygmaLo, \numLrcSygmaHi]$ and $\numLrcGloryxChange$
$[\numLrcGloryxLo, \numLrcGloryxHi]$. Both are real and both are small, and the reading that
matters is that a bank carrying no borrowed template at all is available at under one point of
reach. \CuratedProvenance{}

One further fact about the curated collections bears on how the comparison in the main text is read.
\numSygmaInside{} of SyGMa's \numSygmaRules{} published rules are present in this bank
verbatim: \numSygmaSharePct{} of SyGMa's rule set, and \numSygmaOfcuratedPct{} of the
\numCuratedTotal{} templates nominally carried as curated.
All \numSygmaIncurated{} fall among the named collections and \numSygmaInmined{} in the mined
half, so the containment is inherited from an expert collection rather than rediscovered by
mining. Absorbing a published rule set is ordinary practice and is not the point. The point is
that the two banks in the comparison are not independent objects, which bears on how the result reads: SyGMa's lead at the tightest budgets is a ranking result over
chemistry the two systems largely share, and this work's advantage at the wider ones is that
shared chemistry plus the \numBankRules{} minus \numSygmaInside{} further templates around it.
The \numSygmaOutside{} SyGMa rules that are absent are the second half of the same fact and are
not claimed as coverage.

Mining locates a ring-aware maximum common substructure for each annotated substrate--product
pair. Atoms whose element, formal charge, hydrogen count, aromaticity or degree changed, atom
pairs whose bond order changed, and leaving or entering atoms are flagged as the reaction centre.
The centre is expanded by one bond and emitted as a mapped SMIRKS template. A template survives
only if applying it to its own source substrate regenerates the annotated product after
canonicalisation, and a selectivity filter rejects templates producing more than 200 distinct
products on average across a 50-substrate sample.

That bar bounds how unspecific a template may be; it does not make the bank specific, and the
distribution is worth reporting beside it. Counting the atoms on each template's reactant side,
\numRsizeLetwo{} of \numRsizeParsed{} parseable templates, \numRsizeLetwosharePct{}, have two
atoms or fewer and \numRsizeLethree{}, \numRsizeLethreesharePct{}, have three or fewer. A
template of that size matches almost anywhere it can match at all, so for those the site
discrimination is supplied by the learned stages rather than by the template. That qualifies
the claim that a prediction is rule-attributable without withdrawing it: the rule and the firing
atoms are still reported, and what is weaker is the rule's own selectivity. The tail is not
marginal in practice: \numRsizeCasetop{} of the twenty highest-ranked candidates in the worked
example come from templates with three reactant atoms or fewer.

Support across mined templates is heavily skewed: \numMinedSingleton{} of \numMinedRules{} rest on
a single distinct training pair, and \numMinedSingletonoccurrence{} on a single occurrence of one,
the difference being templates whose catalog entry lists the same pair more than once. The first is
the count that bears on saturation, because withholding a pair withholds every occurrence of it,
and it is the one Section~\ref{sec:si-rarefaction} uses. Re-running the miner on the same split
re-derives every template it derived before and none new, which shows it is deterministic and
nothing more.

\section{What the rule budget buys}
\label{sec:si-budget}

The interactive mode applies \numHOneZeroTopk{} templates because that is the value the released
checkpoint was trained at. That is a fact about how the run was configured and not an argument for
the number, so the curve is measured: validation pools built at a range of budgets with the pool
cap, the fusion, the filter and the matching criterion identical, on \numBudgetSubstrates{}
substrates. Validation and not the comparison set, because a budget chosen on the population the
comparison is reported on would be chosen on the test.

That the model is identical across the points is checked and not assumed, because it is the one
thing a pool did not record. Both scores in a pool are deterministic functions of the substrate
and the candidate, so the models that wrote them can be recovered: scoring a pool's own candidates
with every trained checkpoint held here reproduces its generator scores under exactly one file and
its filter scores under exactly one file, with every other checkpoint disagreeing on nearly all of
them. Every point of this curve resolves to the deployed pair on both stages, two runs that ship a
byte-identical generator counting as one file rather than as an ambiguity, and the producer of the
curve refuses to write unless that measurement exists, covers every pool it reads and returns the
deployed run for all of them. A curve whose points differ in the model as well as in the parameter
measures the model, and nothing else here would have said so.

\input{si_table_budget}

The curve runs to the whole bank. Stopping it at a rule budget of fifty would have left the
sweep short of the configuration the exhaustive mode actually applies, and the comparison between
the two modes then rests on two points measured on different populations rather than on one curve.

The deployed value sits at the knee. Both smaller budgets lose to it at a budget of fifteen with
the interval excluding zero, and the next larger one gains $+\numBudgetKFiveZeroGap$
$[\numBudgetKFiveZeroLo, \numBudgetKFiveZeroHi]$, an interval covering zero, while returning
\numBudgetKFiveZeroCandidates{} candidates against \numBudgetKThreeZeroCandidates{}. The whole
bank gains $+\numBudgetKSevenFiveEightOneGap$ $[\numBudgetKSevenFiveEightOneLo,
\numBudgetKSevenFiveEightOneHi]$ on the same terms, an interval that also covers zero, for
\numBudgetKSevenFiveEightOneCandidates{} candidates, close enough to the pool cap of
\numHNineCap{} that the cap sets the list length on most substrates. Every one of those
candidates is standardised, which the runtime section identifies as the binding cost, and no rule
budget up to and including the whole bank beats the deployed one at an output budget of fifteen,
which is where the headline is read.

That is not the whole story, and the column for an output budget of thirty makes it plain: read
there, the larger rule budget does buy something, because the pool it builds has not run out. The
whole bank reaches \numBudgetKSevenFiveEightOneRecallthirty{} there against
\numBudgetKThreeZeroRecallthirty{}, a paired gain of $+\numBudgetKSevenFiveEightOneGapthirty$
$[+\numBudgetKSevenFiveEightOneLothirty, +\numBudgetKSevenFiveEightOneHithirty]$ and the largest
step in the column, which is the exhaustive mode's advantage measured as a point on this curve
rather than as a separate design. The contrast is computed at both output budgets the table is
read at, because a recommendation that rests on two point estimates is the thing this work asks
other papers not to make. A deployment
answering with thirty or fifty candidates should raise the rule budget for exactly the reason the
Discussion gives for the interactive mode's deficit there, and pay the standardisation cost that
follows.

One substrate of the validation draw does not finish the whole bank in any time worth spending,
so the curve is paired on the \numBudgetSubstrates{} substrates every budget holds and that one
sits outside it, carrying \numBudgetExcludedrefsWord{} references. What the absence could have
done is bounded rather than noted: giving those references entirely to one arm and entirely to
the other moves the whole-bank contrast between $+\numBudgetKSevenFiveEightOneBoundworst$ and
$+\numBudgetKSevenFiveEightOneBoundbest$ read at fifteen, and between
$+\numBudgetKSevenFiveEightOneBoundworstthirty$ and $+\numBudgetKSevenFiveEightOneBoundbestthirty$
read at thirty, which is the reading the recommendation rests on. Across both columns the absence
changes the sign of \numBudgetFragileWord{} contrast, a rule budget of fifty read at fifteen,
whose interval covers zero either way. No verdict read from this table turns on the substrate
that is missing from it.

\section{The drawing's effect where the drawing changes anything}
\label{sec:si-dialcond}

The sweep over how the substrate is drawn reports its effect pooled over the whole comparison set.
That is the right number for reading the comparison, because the comparison is over the whole set,
and it is the wrong number for a user, because most substrates in that set carry no amide: their
two drawings are the same molecule and contribute a difference of exactly zero. The standardiser
changes \numDialcondChanged{} of \numDialcondSubstrates{} substrates, so more than seven in ten of
the pooled figure is made of guaranteed zeros.

Conditioned on the substrates it changes, and on the same pools, the exhaustive arm loses
$\numDialcondConditional$ of micro recall at a budget of 15,
$[\numDialcondConditionalLo, \numDialcondConditionalHi]$, against $\numDialcondPooled$
$[\numDialcondPooledLo, \numDialcondPooledHi]$ pooled. The conditional effect is the larger by
nearly three times, on a population a quarter the size. Neither separates from zero at this budget:
both intervals close on zero rather than near it, and the artifact records that verdict itself
rather than leaving it to be read off a bound that rounds to nothing. Of the \numDialcondNbudgetsWord{} budgets swept, the two
effects separate together at \numDialcondBothsepat{} and at no other.

Both figures belong in the record and they answer different questions. A reader deciding whether
the drawing moved a verdict wants the pooled one. A chemist submitting one substrate wants the
conditional one, because their substrate is either affected or it is not.

\section{Whether the missing chemistry is the corpus's or the bank's}
\label{sec:si-typeoverlap}

Section~\ref{MS-sec:results} measures that most uncovered references need a transformation type the
bank does not hold, and the manuscript states that the shortfall is a property of the corpus. Those
are two propositions. Both are counted over types, and typing is not byte-reproducible for the
reason Section~\ref{sec:si-oracle} gives: the maximum common substructure search runs under a
wall-clock timeout and a cancelled search is untypeable, so repeated runs differ in a small number
of typings. The share this section reports is stable to four decimals across the four coarsenings
it is varied over, which is a wider perturbation than that instability, but the counts here should
be read with the same tolerance. The bank is mined from the training split and a template survives only if it
regenerates its own product and passes a selectivity filter, so a type can sit in the training
annotation and never reach the bank; a shortfall of that kind is the miner's.

They separate by typing both halves of the annotation under the definition the census uses and
intersecting the results, which needs no record of which references were uncovered. The training
split's \numTypeoverlapTraintypes{} distinct types are set against the evaluated test set's
\numTypeoverlapTesttypes: \numTypeoverlapShared{} occur in both and
\numTypeoverlapAbsenttypes{} only in test. Counted by reference rather than by type, because a
type carrying many references matters more than one carrying a single reference,
\numTypeoverlapPresentrefs{} test references have a type the training annotation contains and
\numTypeoverlapAbsentrefs{} do not, \numTypeoverlapAbsentsharePct{} of the
\numTypeoverlapTyped{} that could be typed.

Those two counts are not nested and neither bounds the other, so the question is settled by
crossing them with a third set rather than by comparing their sizes. A template can be typed
without being applied to anything, so the bank's own \numTypeoverlapBanktypes{} types are cheap to
obtain, and every test reference falls into one of four cells:

\begin{center}
\begin{tabular}{lr}
\toprule
 & references \\
\midrule
type in the bank and in training & \numTypeoverlapInboth{} \\
type in the bank, not in training & \numTypeoverlapInbankonly{} \\
type not in the bank, in training & \numTypeoverlapMinerslost{} \\
type in neither & \numTypeoverlapCorpuslackstoo{} \\
\bottomrule
\end{tabular}
\end{center}

Of the \numTypeoverlapBanklacks{} references whose type the bank does not hold,
\numTypeoverlapCorpuslackstoo{} have a type the training annotation does not hold either:
\numTypeoverlapCorpussharePct{} of the bank's type gap is the corpus's and
\numTypeoverlapMinerssharePct{} the miner's. That is the containment the manuscript's claim needs,
measured rather than argued from magnitudes.

The definition of a type is itself a choice, and this is the claim that reaches the Conclusions, so
it is varied the way the singleton tail is varied in Section~\ref{MS-sec:results}. Under the
\numTypeoverlapGranularitiesWord{} definitions the share is
\numTypeoverlapCorpussharePct{} at the exact multiset,
\numTypeoverlapCoarsesharePct{} with counts dropped, and does not fall below
\numTypeoverlapSharemin{} at either coarser level, where a type no longer determines a product.
The conclusion does not turn on the granularity.

Two cautions belong with it. A type absent from the training annotation is absent from this
corpus's training half and not from chemistry; that is exactly the sense in which the claim is
about a corpus and not about metabolism.

That raises the question the corpus's own assembly puts to this section. The selection that drew
records from the four sources is unrecoverable (Section~\ref{sec:si-corpus}), and it dropped
rather more than a third of one source. If it dropped a \emph{class} of chemistry, it would
manufacture this shortfall rather than reveal it, and the claim above would be about a filter
nobody can read. One of the four sources is on disk in full, so the question can be put to it.
Both halves of MetXBioDB are typed under the same instrument: of its \numMetxdropPairs{} distinct
pairs, \numMetxdropInside{} are inside the corpus and \numMetxdropDropped{} are not, and
\numMetxdropTypedkept{} and \numMetxdropTypeddropped{} of them respectively can be typed.

The filter was not blind to chemistry. The total variation distance between the two type
distributions is \numMetxdropTv{}, and a permutation test over \numMetxdropPerm{} shuffles of
the kept and dropped labels puts the chance of a distance that large at
$p=\numMetxdropP$. \numMetxdropDroppedtypesabsent{} of the dropped pairs,
\numMetxdropDroppedtypesabsentsharePct{}, carry a type the kept half never shows.

It nonetheless does not explain the shortfall, and that is the number that decides the question.
Of the \numMetxdropAbsentcell{} test references whose type neither the bank nor the training
annotation holds, \numMetxdropRecoverableWord{} has a type this source held and the corpus
dropped, \numMetxdropRecoverablesharePct{}; with bond counts ignored,
\numMetxdropRecoverablecoarse{}, \numMetxdropRecoverablecoarsesharePct{}. The selection removed
chemistry, and it did not remove this chemistry.

Two of the four sources can be read this way, not one, and the second is the smaller and the
weaker. Of the GLORYx reference set's \numGloryxdropPairs{} distinct typed pairs
\numGloryxdropInside{} are inside the corpus, and the two halves are further apart than
MetXBioDB's: a total variation distance of \numGloryxdropTv{} with
\numGloryxdropAbsentsharePct{} of the dropped pairs carrying a type the kept half never shows.
The same permutation test returns $p=\numGloryxdropP$ and does not separate, and the reason is
the size of the set rather than the size of the effect: \numGloryxdropKeptp{} typed pairs on one
side and \numGloryxdropDroppedp{} on the other spread over \numGloryxdropTypes{} types leave
almost every type a singleton, so a shuffled label reaches nearly that distance too. It points
the same way as the first source and settles nothing on its own.

The two counts of this set are counted differently and neither fraction may be read against the
other. Section~\ref{sec:si-corpus} keys a pair on the InChIKey skeleton and finds
\numAssemblyGloryxpairsinside{} of \numAssemblyGloryxpairs{} inside; the typing used here keys it
on the transformation type and finds \numGloryxdropInside{} of \numGloryxdropPairs{}. A reader
would not otherwise suspect two instruments, because the source that can be checked against both
gives one answer either way: MetXBioDB returns \numMetxdropPairs{} pairs with
\numMetxdropInside{} inside under each.

One thing had to change to measure it at all. The common-substructure search the typing runs
carries a five-second deadline, and on a source of drug-sized molecules a pair that misses it
types as nothing, so the counts moved with how loaded the machine was: three runs of this source
typed different numbers of its pairs and moved the shares computed from them with it. The deadline is a
parameter here and this measurement is taken at \numGloryxdropDeadline{} seconds, at which two
independent runs agree in every field. MetXBioDB is unaffected: at the mining route's own
deadline it reproduces every figure above exactly.

What bounds the answer is that two sources of four can be read this way, so this closes the
alternative for half of the corpus's provenance and not for all of it. ChEMBL and DrugBank are
the other two and neither is held here.

And the counts in this section are made by one instrument and are not interchangeable with the
counts elsewhere. Typing by the radius-0 route leaves \numTypeoverlapUntypeable{} of the
\numHydReferences{} references untyped; the composite instruments of
Section~\ref{sec:si-refstep}, which need a reaction centre rather than a type, leave a different
number; and the census of Section~\ref{MS-sec:results} reports its untypeable count within the
uncovered subset rather than over the whole set. Three numbers describing three measurements,
named here because printed side by side they read as one measurement disagreeing with itself.

\section{Whether template discovery has saturated}
\label{sec:si-rarefaction}

Re-running the miner on the same split re-derives every template and none new, which is a
determinism check and not a saturation one. Saturation is a claim about what more data would
yield, and it can be measured on files already held: the mining catalog records which training
pairs each template was derived from, so pairs can be withheld and the templates that would still
have been found counted.

Of \numRareTemplates{} mined templates over \numRarePairs{} distinct training pairs,
\numRareSingletons{} rest on a single pair, \numRareSingletonsharePct{}. A quarter of the pairs
yields \numRareQuarter{} templates, half \numRareHalf{}, three quarters
\numRareThreequarters{} and the whole set \numRareAll{}, so the curve is close to linear and at
its right-hand end is still gaining \numRareSlope{} templates per thousand further pairs. Good and
Turing's estimate of the probability mass belonging to types not yet seen is
\numRareUnseenmass{}. On every one of those readings the population is undersampled rather than
exhausted.

What that does not establish is that mining harder would raise the ceiling. The shortfall
Section~\ref{MS-sec:results} reports is transformation types the corpus does not contain, and no
template mined from a corpus can supply a type absent from it. The two statements are
compatible and only the first was measured; the manuscript claims only the first.

\section{Two parameters of the ranking, swept}
\label{sec:si-aggregation}
\label{sec:si-knobs}

Two settings of the ranking are at a default that no measurement here chose, and this work's own
argument makes both worth turning.

The fusion constant is the first. Recomputing the fusion from the stored component scores costs no
model run, so it is swept over \numFusionkSweptWord{} values spanning two orders of magnitude, on
the \numFusionkSubstrates{} substrates of the comparison set carrying \numFusionkReferences{}
references. The \numFusionkSeparatingWord{} smallest constants separate against the deployed
$K = \numFusionkDeployed$ at a budget of 15, by up to $\numFusionkWorst$; every constant of
\numFusionkFlatfrom{} or more does not.

That last is a null, and this work states a null as the bound it is rather than as an absence. Over
the constants at or above \numFusionkFlatfrom{}, no paired difference against the deployed value
reaches beyond $\numFusionkBandOneFive$ at a budget of 15, $\numFusionkBandFive$ at 5,
$\numFusionkBandThreeZero$ at 30 or $\numFusionkBandFiveZero$ at 50. The band at fifteen is
of the same size as the smaller effects this paper reports as real, so what the sweep establishes
is that the constant is not worth a percentage point, not that it does nothing.

At one budget it is not a null at all. $K = \numFusionkBreakconstant$ falls
$\numFusionkBreakgap$ $[\numFusionkBreaklo, \numFusionkBreakhi]$ short of the deployed value at a
budget of 30, which is \numFusionkNullbreaksWord{} separating cell of the twelve in the lower
panel of Table~\ref{tab:si-fusion-k}. It runs in the deployed value's favour, so the published
default sits on the flat part of the curve where the curve is flat and above it where it is not,
and leaving it there costs nothing measurable at three of the four budgets and gains
$\numFusionkBreakgapabs$ at the fourth. That is a weaker claim than the one this section used to
make and a checkable one.

Measuring the sweep on the population this paper reports is not the objection
Section~\ref{sec:si-aggregation} raises against the aggregation blend. That objection is against
\emph{adopting} a parameter chosen here; this sweep adopts nothing, since the constant stays at
the value the method was published with and the sweep found no reason to move it. Had it found one,
the same validation re-run the blend required would have been owed. The values and the levels are
Table~\ref{tab:si-fusion-k} rather than a description of them.

\input{si_table_fusion_k}

The second is the aggregation rule. Equation~\ref{MS-eq:noisyor} combines the templates that
reach a candidate by noisy-or, which treats them as independent evidence. This bank does not
satisfy that: it contains \numThirdpartyBiotransformerShipped{} of BioTransformer's published
templates and \numSygmaInside{} of SyGMa's verbatim, and the mined half is full of
near-duplicates, so a candidate that many templates reach is scored as though many witnesses had
agreed. The released pools carry one aggregated score per candidate and not the per-template
scores a different rule needs, so the question cannot be answered from them.

They are re-derived here. The generator side is run again over the comparison set with the
per-template scores kept rather than collapsed, so any aggregation rule can be evaluated without
another pass; the filter is not re-run, because a filter score is a function of the substrate and
the product and does not depend on how the generator combined its templates, so the frozen scores
are joined by structure. \numAggregationJoined{} candidates join and
\numAggregationUnjoined{} do not, and those are dropped from every arm alike. The deployed
rule re-derived this way reproduces the whole-bank column of Table~\ref{MS-tab:sweep} at every
budget, which is what certifies that the re-run is on the deployed model; the artifact refuses to
report if it does not.

\input{si_table_aggregation}

The alternative the violation argues for is a maximum, which counts a candidate once however many
near-duplicate templates restate it. It moves the ordering, and where it moves it is where the
violation predicts. It gains at the head of the list, $+\numAggregationMaxOne$
$[+\numAggregationMaxOneLo, +\numAggregationMaxOneHi]$ at a budget of one and
$+\numAggregationMaxFive$ $[+\numAggregationMaxFiveLo, +\numAggregationMaxFiveHi]$ at five,
both intervals excluding zero, and it is worse where this work reads its leads: at thirty it is
$\numAggregationMaxThreeZero$ $[\numAggregationMaxThreeZeroLo,
\numAggregationMaxThreeZeroHi]$, an interval excluding zero on the other side. The implementation also offers a blend of the two,
\begin{equation}
  p_c^{\mathrm{blend}} = \numAggregationBlendmax{}\,\max_{r \in R(c)} p_r
  + \numAggregationBlendor{}\Bigl(1 - \prod_{r \in R(c)}(1 - p_r)\Bigr),
  \label{eq:blend}
\end{equation}
with $R(c)$ and $p_r$ as in Equation~\ref{MS-eq:noisyor} and the two weights fixed in the
implementation rather than fitted here. It gains at every budget through ten, by at most
$+\numAggCmpHybridMax$ $[+\numAggCmpHybridMaxLo, +\numAggCmpHybridMaxHi]$ at a budget of
\numAggCmpHybridMaxkWord{}, and is indistinguishable from the deployed rule at every wider one.
That last clause is a statement about this population and not about the rule. On the validation
draw the same blend separates at every budget from \numAggValHybridSepfromWord{} to
\numAggValHybridSepto{}, reaching $+\numAggValHybridMax$ $[+\numAggValHybridMaxLo,
+\numAggValHybridMaxHi]$ at \numAggValHybridMaxkWord{}, so it is distinguishable there at two of
the budgets at which this work claims a lead. The two populations disagree about the deployed rule,
which is the reason Section~\ref{MS-sec:limitations} recommends the blend to a deployer rather than
reporting it as a settled improvement. The mean is the one rule that is worse at the wide budgets,
by $\numAggregationMeanThreeZero$ $[\numAggregationMeanThreeZeroLo,
\numAggregationMeanThreeZeroHi]$ at thirty, with an interval excluding zero at every budget from
fifteen upward.

Three things follow. The independence assumption is false, something does turn on it, and what
turns on it is the head of the list rather than the wide budgets this work reads its leads at:
where a duplicate-blind rule helps, it helps a chemist reading the first three candidates.
Nothing claimed here changes, because neither rule the violation argues for is better than the
deployed one where a lead is claimed: the blend is indistinguishable from it at every such budget
and the maximum is worse at thirty with the interval excluding zero. And this sweep alone cannot move the deployed rule: it was run on the comparison set after the
comparison, so adopting the blend on the strength of it would be selecting a parameter on the
population the paper reports, which is the one thing this work's own discipline forbids. What it
licenses is a selection on validation, and that selection was made.

\paragraph{The same sweep on validation.} The four rules are compared again on the validation
draw, \numAggvalSubstrates{} substrates carrying \numAggvalReferences{} references, with
everything but the rule held as before. There is no published column for this population, so the
re-run cannot be gated on reproducing one; it is checked against the curve of
Section~\ref{sec:si-budget} instead, which reads the same pools at the whole bank. The re-derived
deployed rule agrees with that curve at \numAggvalCheckagreeWord{} of the
\numAggvalCheckbudgetsWord{} budgets both report and differs by \numAggvalCheckworst{} at the
sixth, which is the size of the only difference in construction between them, the candidates the
join drops. The two populations agree on the direction and validation
is the stronger of the two. The blend beats the deployed rule with the interval excluding zero at
\numAggvalHybridwinsWord{} budgets, every one from $1$ to \numAggvalHybridwinsmax{}, by as much
as $+\numAggvalHybridOneZero$ $[+\numAggvalHybridOneZeroLo, +\numAggvalHybridOneZeroHi]$ at ten,
and it is indistinguishable from it at $30$ and $50$. Recall at a budget of five goes from
\numAggvalDeployedrecallfive{} to \numAggvalHybridrecallfive{}. The maximum separates over the
same head of the list and the mean is worse almost everywhere, both as on the comparison set.

Two things follow and they should be stated together. The deployed aggregation is not the one a
selection on validation would choose for a service answering with a short list: on the population
where such a choice is legitimate, the blend is better through a budget of
\numAggvalHybridwinsmax{}, which includes the budget this paper reads its headline at. And every
number in this paper is measured under the deployed rule, so the release keeps it and the finding
is reported rather than acted on here; substituting the blend would mean re-measuring the whole
comparison, the ceiling and the worked example under a configuration this paper has not evaluated.
What the measurement establishes is that the independence assumption is false, that something
turns on it, and that the correction it argues for is worth an amount this work can now state
instead of guess.

\section{Model architectures and training}
\label{sec:si-arch}

\subsection{Generator}

The generator estimates $P(r \mid s)$, the probability that rule $r$ applies to substrate $s$,
for the whole bank in one forward pass. Substrate graphs carry 16-dimensional node and
18-dimensional edge features. A three-layer GATv2 encoder ($16 \to 192 \to 256 \to 128$, batch
normalisation, ReLU, dropout $0.1$, mean-pooled readout) embeds the substrate, and a parallel
multilayer perceptron embeds a 1024-bit Morgan fingerprint, the two combined by a gated fusion.
Each rule is embedded independently by a rule-graph encoder of the same family
($128 \to 192 \to 128$) alongside a six-dimensional branch of global rule features.

The logit sums four learned terms, each with its own learned scalar: a cross-attention term in
which the rule embedding queries the substrate's atom states through single-head attention; a
global term, the cosine similarity between substrate and rule embeddings; a local term, the cosine
between the attention context and the rule; and a term reflecting whether the rule's SMARTS
matches. To these are added a per-rule bias and a frequency-prior term at weight $0.4$, the
log-odds that the rule yields a true metabolite estimated from training statistics. A penalty of
$7.5$ is subtracted for rules whose SMARTS does not match, which masks inapplicable rules before
the sigmoid.

Training is multi-label rule classification with a margin-ranking auxiliary. Every setting either
component was trained under is in Table~\ref{tab:si-hyperparameters}, read from the configuration
each run recorded beside its weights rather than transcribed. Both released checkpoints come from
one run, the one whose label matrix was built in the firing convention, and that is established
rather than recalled: scoring a pool's own candidates with every trained checkpoint held here
reproduces its generator scores under that run's generator and no other, and its filter scores
under that run's filter and no other (Section~\ref{sec:si-prov}).

\input{si_table_hyperparameters}

\subsection{The gate the release applied and the measurement did not}

One of the settings in that table describes a selection no number here is measured under, and it
is worth separating from the ones that are. The generator checkpoint carries a calibrated rule
threshold of \numDefaultgateThreshold{}, a gate that drops a rule scoring below it before the
rule budget is applied. Every pool this paper is scored on is built with no such gate: the
selection is the top of the score ranking cut at the budget, and nothing else.

The two are not the same selection. On the comparison set a median of \numDefaultgateClearing{}
rules clear the gate against a budget of \numDefaultgateBudget{}, and it binds on
\numDefaultgateBinds{} of \numPopulationN{} substrates, \numDefaultgateBindsharePct{}. Because
both orderings are by the same score, the gated set is a prefix of the ungated one wherever the
gate binds, so it removes candidates the evaluated arm has and never adds one. A caller who ran
under the gate would therefore be running a narrower configuration than any arm reported here,
and no number in this paper describes it.

The release is the evaluated configuration: an unspecified rule threshold means no gate, and the
checkpoint keeps the value so a caller who wants the gate can pass it. The same holds for the
evaluation path a reader would use to select a configuration of their own, since selecting on a
setting the release does not run is the same defect one layer up. A regression test holds all
three together, because a released default that differs from an evaluated one is invisible to
every other check here and is the defect this paper argues about.

\subsection{Filter}

The filter scores a (substrate, candidate) pair. Substrate and candidate are embedded
independently, each through its own three-layer GATv2 encoder of the same dimensions with a
mean-pooled readout. The two 128-dimensional embeddings are concatenated with both Morgan
fingerprints, and the resulting 2304-dimensional vector is mapped through a three-layer perceptron
($\to 128 \to 64 \to 1$) to a single logit. The estimator is selectable between a non-negative
positive-unlabelled risk on logits and binary cross-entropy on probabilities; the released
configuration uses the second.

A variant encoding one merged substrate--product graph, with cross-edges placed by an
element-aware maximum-common-substructure correspondence, was implemented and measured. It loses
on accuracy (recall@15 $0.357$ against $0.366$, paired difference $-0.0090$, 95\% CI
$[-0.017, -0.001]$, $n = 1{,}170$) and on cost ($34.0$~ms against $9.1$~ms per pair), and is not
deployed.

\subsection{How the incomplete annotation is handled}

An annotated metabolite is a positive and a rule-applicable product without an annotation is
unlabelled, not negative, because a missing annotation does not establish that a transformation
does not occur \citep{Jain_2016}. The two learned components answer that differently, and the
released configuration of each is what follows.

The generator down-weights rules that match the substrate but carry no annotation, by half rather
than treating them as negatives, so the label semantics reach the objective directly. The filter
does not: it can be trained under a non-negative positive-unlabelled risk, and the released
checkpoint is trained under cross-entropy instead, so its unlabelled pairs enter as negatives. The
consequence is that its scores are conservative on exactly the candidates the annotation is silent
about. The same silence makes precision pessimistic by a bounded but unknown amount, which is why
the paper reports it without using it to order systems.

That leaves an ablation this work's own framing asks for and does not report: the same filter
trained under the positive-unlabelled risk instead, scored the same way. It is not a re-ranking
of stored scores, which is why it is absent rather than deferred; it needs a training run at the
deployed configuration and a re-scoring of every pool from it. That is the same run the
retraining spread needs (Section~\ref{MS-sec:limitations}), so one campaign would settle both,
and neither is settled here. What can be said without it is which estimator the release uses and
in which direction its bias runs, and both are stated above.

\section{Whether the site a prediction names is the site that changed}
\label{sec:si-site}

Every prediction carries the template that produced it and the atoms that template fired on, and
the main text offers that as a reason to prefer this system. A rule can reach the right product
from the wrong place, so the claim is testable and had not been tested.

For every candidate the generator produces whose key matches an annotated reference, the atoms
fired on are compared with the atoms that differ between substrate and reference. The comparison
is made before the filter and before any budget, so what is measured is localisation and not
ranking. Of \numSiteScored{} such matches on the comparison set, the reported site touches the
change in \numSiteTouchingPct{} and lies wholly inside it in \numSiteInsidePct{}. The second
figure is lower for a reason that is not a failure: a template matches its whole pattern, which
is larger than the part it alters.

What the change is, exactly, is inferred and not annotated. The corpus records a substrate and a
product and no site, so the reference centre here is derived: the common substructure of the two
is computed, the substrate atoms it does not cover are taken as changed, the substrate atoms
carrying a part of the product the substructure does not cover are added, and the neighbours of a
removed atom with them. That is a structural inference. What this establishes is agreement between
the site a prediction names and the change it claims to explain, not agreement with an
experimentally determined site of metabolism, for which this corpus has no labels.

The figure needs a floor, because both sets are subsets of the same molecule and on a small
substrate with a large centre they would meet whatever the rule did. Two floors are measured. A
set of atoms of the same size drawn uniformly touches the centre \numSiteNullPct{} of the time, so
the reported sites exceed it by \numSiteMarginPct{}. That null is the wrong shape: a template match
is a connected fragment, not a scatter. Growing a fragment of the same size along bonds from a
random seed atom touches the centre \numSiteConnectednullPct{} of the time, and the reported sites
exceed that by \numSiteConnectedmarginPct{}.

The connected null is the lower of the two, which is the opposite of what we expected and worth
saying: a scattered set of atoms reaches into more of the molecule than a fragment clustered in one
place, so it meets a given centre more often. Against the null the comparison actually needs, the
margin is wider than the uniform one suggested. The reading remains that a raw agreement rate near
nine in ten is not the size of the effect; the size is the margin, and it is
\numSiteConnectedmargin{}.

\section{What a single step is}
\label{sec:si-singlestep}

The paper's predictions are single-step in the sense that one template is applied once. Whether
that is also one enzymatic step depends on the templates, and it is not always.

Every mined template comes from an annotated substrate--product pair, so the question can be put
to molecules rather than to templates. Two instruments do so, and neither alone is enough.

\paragraph{The first instrument: how many loci.} The reaction centre of the source pair, located
by the same maximum-common-substructure route the transformation types use, falls in more than one
piece for \numCompositeLoci{} of the \numCompositeScored{} mined templates,
\numCompositeLocisharePct{}. One mined template could not be scored, so the denominator throughout
this section is \numCompositeScored{} and not \numMinedRules{}. A centre in two pieces is one event when a bond appears or disappears
between them, which is what a cleavage or a cyclisation looks like, and two independent edits
otherwise; this instrument counts the pieces and does not separate the two cases. It has a blind
spot in the other direction as well, and the blind spot is the larger problem: two edits that
share or neighbour centre atoms arrive as one piece and are counted once. This share is therefore
a floor and not a bound.

\paragraph{The second instrument, and its bar, fixed first.} The remedy is a second lens with no
threshold chosen after the fact, so the threshold was registered as H16 before the instrument was
written. On the same pair, let \emph{core} be the matched atoms and count $E$: every bond between
two core atoms whose presence or order differs, plus every attachment point at which a substituent
is gained or lost, each once. Bonds internal to an added or removed fragment are not edits to the
substrate and are not counted, so a glucuronide costs one and not twelve. The cut is set from
chemistry rather than from a histogram: a conjugation breaks one bond and forms one, $E=2$; an
epoxide hydrolysis reaches $E=3$; an aromatic hydroxylation through the arene oxide within one
enzyme reaches $E=4$ at worst. Nothing one enzyme does in one turnover reaches five, so the
threshold is $E \ge \numCompositeThreshold{}$.

\paragraph{What the two find together.} The second instrument flags \numCompositeEdits{}
templates, \numCompositeEditsharePct{}. The two agree on only \numCompositeBoth{}, which is the
point of having both: each sees most of what the other misses. Their union is
\numCompositeUnion{} templates, \numCompositeUnionsharePct{} of the mined templates. The registered bar
was \numCompositeBarPct{} and the prediction is confirmed. The composite share of the mined bank is
therefore about a fifth, not the \numCompositeLocisharePct{} one instrument reports, and both counts
are reported here, so that the contribution of the instrument itself is visible.

Inspection of the flagged pairs finds both kinds. Among the composites are a pair whose product
carries two new carbonyls on one piperazine ring, a pair in which a ketone is reduced and a
pyridine nitrogen oxidised, and a pair gaining sixteen heavy atoms as a glutathione conjugate
after a substitution. None of these is one enzyme acting once.

The consequence for the paper is confined and stated where it bites: the coverage ceiling is a
one-template ceiling. A rule that performs two edits can reach a metabolite two steps away in one
application, so the ceiling is not the reach of one enzymatic step, and the depth-two experiment
in the Limitations measures a second template application rather than a second enzymatic step. The
corrected share also explains that experiment's small effect. If a fifth of the mined bank
already performs two edits in one application, as P10 finds, a second application has little left
to discover that the first did not already reach, and the observed lift of about $0.012$ is what
that predicts rather than a surprise.

The instrument reads the source pairs and not the templates, and that is deliberate: a cleavage
whose bridging atom carries no map number is invisible to a comparison of mapped bonds, and a
cyclisation joins two distant loci in a single event, so parsing partially mapped SMIRKS answers a
different question from the one asked and answers it differently from what the molecules show.

\section{How much of the annotation is one enzymatic step}
\label{sec:si-refstep}

Section~\ref{sec:si-singlestep} measures how many mined templates perform more than one enzymatic
edit and draws the consequence for the coverage bound. That measurement is over templates. The
question a reader of a recall figure has is about the references, because a corpus recording a
metabolite several steps from its parent asks a predictor for something no single application can
be, and counts a hit when one template stands in for the lot.

The same two instruments are therefore applied to every annotated pair of the evaluated test set.
Of the \numRefstepTyped{} pairs that can be typed, \numRefstepLoci{} have a reaction centre falling
in more than one connected locus and \numRefstepEdits{} are composite by core-incident edit count
at the registered threshold; their union is \numRefstepEither{},
\numRefstepEithersharePct{} of the annotation.

Nothing in that makes a composite reference unreachable, and it is worth being clear about why: a
template mined from a composite pair performs the whole transformation in one application, which is
how the bank reaches many of them and why the ceiling is as high as it is. What the figure settles
is what a recall number means. Roughly one annotated metabolite in six is not a single enzymatic
step, so the same is true of roughly one hit in six, and a reader who takes recall for the share of
one-step chemistry a system finds will be wrong by about that much.

\section{The registered predictions}
\label{sec:si-register}

The register is released in full at \texttt{github.com/doctawho42/GRAIL}, with every prediction as
it was written, the threshold and falsification condition fixed before the check, and the outcome
as it was measured. Sixteen predictions are registered. Ten are reported in the paper as P1 to
P10; the register's own identifiers are given beside them in the main text, since its numbering
records when each was written rather than where it appears.

P10 is the newest and its order is checkable rather than asserted. Its threshold, its population
and its falsification condition were committed to the register in a commit of their own, and the
instrument that measures it was written afterwards in a later commit. The two commits are in the
released history, in that order, so the sequence is verifiable rather than asserted.

The register has one weakness, stated here. Only P10 fixes its
adjudicating population as part of the pre-specification. The nine before it name a threshold and
a falsification condition and leave the population implicit, and three of them, P5, P6 and P7,
were in the event adjudicated on the comparison set, which is derived from the test split,
while the rest were adjudicated on validation. Table~\ref{MS-tab:hyp} records the population each was checked on
so which is which is on record, but recording it after the fact is not the same as fixing
it before, and for those three it was not fixed before. The register's later entries do fix it.

The remaining six concern work this paper does not report, and they are named here by their
register identifiers, so that none can quietly leave unnoticed: H1, a type-indexed rather
than rule-indexed label space; H2, reactivity and accessibility node features; H3, learned
abstention; H4, a ranked prediction of intervention sizes; H5, the same null out of sample; and
H6, a negative control on graph automorphism. Each is stated with its threshold so that a later
result on any of them cannot be a threshold chosen after the fact. None is reported in this paper
because none was run for it, which is a different thing from a prediction that was run and did
not hold; the three of those are P6, P7 and P8 and they are in Table~\ref{MS-tab:hyp} with their measured
values.

\section{Group ranking and its oracle}
\label{sec:si-oracle}

\input{si_table_oracle}

Candidates group naturally by molecular formula, and an oracle over those groups motivated three
of the registered predictions. Table~\ref{tab:si-oracle} recomputes that oracle under four
partitions of one candidate pool, on the deployed configuration rather than on the uncapped pool
the original measurement used.

\paragraph{How the two failures decompose.} The listwise scorer applied to whole groups (P6)
measured $\numHEightDiff$ against a registered $+\numHEightThreshold$. The deficit splits into
$\numHEightDesign$ from the design, block emission being worse than interleaved
(\numHEightBlocked{} against \numHEightInterleaved{}), and $\numHEightModel$ from the model
itself. Because the oracle above is measured in blocked form,
\numHEightDesignShare{} of the headroom it promises is the return of what the design spends.
The gate before fusion (P7) measured $\numHOneFourDiff$ against a registered
$+\numHOneFourThreshold$ and separated against its hypothesis; a perfect group ranking through
the gate gives $\numHOneFourCeiling$, worse than the generator cap it replaces.

\paragraph{Why the oracle is not an upper bound on the signal.} A binary group ordering entered
as the third term in the fusion reaches \numHOneTwoCeilingrecall{}, the same figure the trained
scorer reaches, which invites reading the binary case as a ceiling. It is not one: a binary
ordering leaves every reference-free group tied, and on validation the trained scorer exceeds it,
$+\numHOneTwoVal$ against $+\numHOneTwoValceiling$. The gain from P5 is also not an artefact of
selection, since all twenty configurations beat the base on validation and seventy per cent of
that margin survived the move to the comparison set.

The transformation type of a substrate--candidate pair is the multiset of bonds that change
between mapped atoms, which is the vocabulary the rule bank's coverage is defined on. Typing runs
a maximum common substructure search under a wall-clock timeout and treats a cancelled search as
untypeable, so the artifact is not byte-reproducible: two runs on the same machine differed in
2 of approximately 29,100 typings, every figure in Table~\ref{tab:si-oracle} was identical to four
decimals, and the only cell that moved was the control at $k=1$, by $0.0015$.

\section{Stage ablations on the deployed configuration}

\input{si_table_ranking}

Table~\ref{tab:si-ranking} is the full budget sweep for the ranking ablation of the main text.
Both learned scorers, their product, the deployed fusion and a seeded permutation order one pool.

Prediction P1 is the fusion-minus-product contrast in that table, and it is given on two
populations. On the validation draw, where the fusion rule had not been selected, it is
$+\numHSevenDeployed$ at a budget of 15. On the comparison set, where the rule was chosen among
four candidates, the same contrast is larger; the difference between the two is the cost of that
selection, and the intervals rather than the point estimates are what settle whether the two
populations disagree. The figure quoted in the main text as $+\numHSevenDiff$ is measured on the
uncapped pool, because P1 was fixed and checked before the pool cap of P2 existed; the capped
figure is the smaller of the two because the cap and the fusion recover overlapping ground.

Two earlier ablations of a superseded configuration, one comparing the candidate scorer against the
frequency prior and one measuring the pool with rule selection switched off, ran on a
245-substrate subsample under the product ordering. Their levels do not transfer to the
configuration reported here. This table and the selection ablation below replace them.

\subsection{Ties in the deployed ranking}

The deployed configuration caps the pool by rule score before the fusion sees it. On a pool
smaller than the cap that truncates nothing, but it fixes the order the fusion reads, and that
matters because the fusion has exact ties: for 14 of the \numPopulationN{} substrates the two
best candidates receive identical fused scores, so which of them is returned first is decided by
the order the pool arrives in rather than by the ranking rule. Fusing the pool as stored instead
of after the cap moves micro recall at $k=1$ from \numSelLearnedOne{} to $0.1068$, a
difference of $0.0015$. Every arm of every ablation here uses the deployed convention, so the
differences reported are differences of method and not of tie-breaking.

\section{When a ranking comparison can measure anything}

Two experiments in this work asked whether a ranking beats an alternative ordering of the same
pool, and the first of them could not have answered. It compared the product ordering against
Tanimoto similarity on a pool whose median size is \numSimOldMedianPool{} candidates, at a budget
of 15. For \numSimOldPoolUnderBudget{} of \numSimOldN{} substrates the entire pool fits inside
the budget, so every ordering returns the same set and no arm can separate from another. The
measured difference, $+\numSimOldGap$ with the interval covering zero, is what that design is
able to produce and not evidence that the two orderings are equivalent.
% no-claim: derivation

The same limit governs a null model of the form used elsewhere in this literature, a uniform draw
of $\min(k, N)$ from a method's own pool of $N$.
% no-claim: derivation
Its lift tends to one as the budget approaches
the pool, so at $k=15$ a method emitting about eleven candidates has a lift of $1.055$ whatever
its ranking is, while a method emitting eighty still has room. Comparing methods by lift at a
fixed budget therefore ranks them partly by how much they emit, which is the same confound the
budget sweep in the main text is designed to expose.
% no-claim: derivation

The rule both cases give is simple and we state it because neither experiment stated it in
advance: a ranking comparison carries information only over the substrates whose pool exceeds the
budget, and the number of such substrates should be reported with the result.
% no-claim: derivation
The comparison in
the main text is run on the deployed pool, capped at \numCaseExhCandidates{}, where the budget is
narrower than the pool for every substrate.

\section{The choice of evaluation population}
\label{sec:si-population}

The comparison set is a quarter of the evaluated test split and not a random quarter. What fixes
it is worth stating precisely, because the obvious reading is wrong: it is not that a substrate
enters only where every method emitted something, a rule that would remove the cases where a
comparator produced nothing and a bank this size should win most clearly. The population is the
intersection of the substrates each method has an \emph{entry} for, and the binding constraint is
one submission list: the comparator that is a web service was sent
\numPopulationN{} substrates and returned all \numPopulationN{}. Nothing was removed for emitting
nothing, and the set still contains substrates a method answers nothing for:
\numPopdefSygmainsideWord{} for SyGMa, \numPopdefMetapredictorinsideWord{} for MetaPredictor,
\numPopdefBtinside{} for BioTransformer and \numPopdefGrailinsideWord{} for the deployed arm.

What that emission rule would have removed depends on which arm is counted. Over the whole
evaluated test set, SyGMa answers nothing for \numPopdefSygmasilent{} of
\numPopulationTestsubs{} substrates, carrying \numPopdefSygmasilentrefs{} references, and
MetaPredictor for \numPopdefMetapredictorsilent{}, carrying
\numPopdefMetapredictorsilentrefs{}. For those two the rule would have removed almost nothing.
BioTransformer is the arm it would have bitten: \numPopdefBtsilent{} substrates carrying
\numPopdefBtsilentrefs{} references, which is \numPopdefBtsilentsharePct{} of the population, and
the rule would have taken that much of the set out of every comparison it appears in.

What the population does carry is an unrecorded draw, and that is tested rather than described.
Against the \numPopdefOutside{} evaluated substrates outside it, the comparison set does not
differ in the number of annotated references it carries
(\numPopdefRefsin{} against \numPopdefRefsout{}, $p=\numPopdefRefsp$), in substrate size
(\numPopdefHeavyin{} against \numPopdefHeavyout{} heavy atoms, $p=\numPopdefHeavyp$), or in how
many candidates the deployed system emits on it (\numPopdefOutputin{} against
\numPopdefOutputout{}, $p=\numPopdefOutputp$), each by a permutation test over
\numPopdefPermutations{} shuffles. That does not make the draw random; it says the three
properties most likely to have driven such a draw did not drive this one.

The comparison is also read on the population nobody selected: the whole evaluated test set,
\numPopdefWholesubstrates{} substrates carrying \numPopdefWholereferences{} references, against
the \numPopdefWholearmsWord{} comparators that ran over all of it, with a substrate a comparator
answered nothing for scored as zero rather than removed. BioTransformer is one of the three: it
is a jar this repository holds and it runs here under the x86\_64 Java 8 runtime its bundled
InChI artefact is built for, so what had kept it off this population was the cost of the run and
that cost was paid. MetaTox is the one arm that is not here. Taking it to this population means a
second submission of the substrates outside the comparison set, it was not made, and the reason
is the cost of a submission rather than an inability: this paper counts two other absences as
unforced for the same reason and this one is the third. Both arms are read there, which matters because the wide-budget
leads this work claims are the exhaustive arm's and that arm existed only on the comparison set
until its pools were built for the whole split.

The interactive arm does not improve on the wider population. Against SyGMa it is
$\numPopdefWholeSygmaOneFive$ $[\numPopdefWholeSygmaOneFiveLo,
\numPopdefWholeSygmaOneFiveHi]$ at a budget of fifteen over the whole set, against
$\numPopdefCompSygmaOneFive$ $[\numPopdefCompSygmaOneFiveLo, +\numPopdefCompSygmaOneFiveHi]$ on
the comparison set: the wider population turns a tie into a small deficit that separates. Against
MetaPredictor neither population separates.

The exhaustive arm's leads mostly survive the move, and the exception is named. Of the
\numPopdefLeadstotalWord{} separating leads it holds on the comparison set against the
\numPopdefWholearmsWord{} comparators that reach the wider population,
\numPopdefLeadskeptWord{} still separate over the whole set and
\numPopdefLeadslostWord{} does not. The \numPopdefLeadstotalWord{} are the exhaustive arm against
SyGMa and MetaPredictor at fifteen, thirty and fifty and against BioTransformer at every budget
this section sweeps, and \numPopdefLeadskeptWord{} of them survive:
against SyGMa $+\numPopdefExhwholesygmaThreeZero$ $[+\numPopdefExhwholesygmaThreeZeroLo,
+\numPopdefExhwholesygmaThreeZeroHi]$ at thirty and $+\numPopdefExhwholesygmaFiveZero$
$[+\numPopdefExhwholesygmaFiveZeroLo, +\numPopdefExhwholesygmaFiveZeroHi]$ at fifty, against
MetaPredictor $+\numPopdefExhwholemetapredictorOneFive$
$[+\numPopdefExhwholemetapredictorOneFiveLo, +\numPopdefExhwholemetapredictorOneFiveHi]$ at
fifteen, $+\numPopdefExhwholemetapredictorThreeZero$ and
$+\numPopdefExhwholemetapredictorFiveZero$ at thirty and fifty, and against BioTransformer at
every budget this section sweeps, where the wider population separates the two more strongly than
the comparison set does.

Two things bound that count and neither is visible from the table. This section sweeps
\numSTwoNineBudgets{} budgets and Table~\ref{tab:si-intervals} sweeps
\numSTwoNineTablebudgets{}; the four it adds, at \numSTwoNineBudgetsunaudited{}, have no
whole-test-set counterpart, so a lead at one of them cannot be followed across the move and is not
in this denominator. Counted over the table's nine instead, the arm holds \numSTwoNineLeads{}
separating leads rather than \numPopdefLeadstotal{}, and that larger figure answers a different
question.

The second is a difference between two runs of one comparator. The columns here are read from the
predictions that also cover the whole test set, so that the move varies the population and nothing
else; Table~\ref{tab:si-intervals} reads the declared arm, which exists on the comparison set only.
For SyGMa and MetaPredictor the two agree. For BioTransformer they do not, and at a budget of
\numSTwoNineBtdisagreeat{} the disagreement is large enough to change whether the cell
separates. Each choice is right for its own purpose and neither is the other's, so a figure from
this section may not be read against one from that table. What does not survive is the
narrower one: the lead over SyGMa at a budget of fifteen is
$+\numPopdefExhcompsygmaOneFive$ $[+\numPopdefExhcompsygmaOneFiveLo,
+\numPopdefExhcompsygmaOneFiveHi]$ on the comparison set and
$+\numPopdefExhwholesygmaOneFive$ $[\numPopdefExhwholesygmaOneFiveLo,
+\numPopdefExhwholesygmaOneFiveHi]$ over the whole set, an interval covering zero. That claim is
therefore restricted to the comparison set wherever it appears. Two cells move the other way for
the same reason and are reported here rather than passed over: at a budget of ten the exhaustive
arm trails SyGMa by $\numPopdefExhwholesygmaOneZero$ $[\numPopdefExhwholesygmaOneZeroLo,
\numPopdefExhwholesygmaOneZeroHi]$ and MetaPredictor by
$\numPopdefExhwholemetapredictorOneZero$ $[\numPopdefExhwholemetapredictorOneZeroLo,
\numPopdefExhwholemetapredictorOneZeroHi]$ over the whole set, both intervals excluding zero,
where on the comparison set neither separates.

A separate analysis re-ran three
methods, four metrics and five criteria on two populations of one split, and asked whether the
ordering of methods changes with the population. The two are the full \numPopulationTestsubs{}
and a \numBtN{}-substrate subset drawn for an earlier evaluation against MetaTrans
\citep{Litsa_2020}, which is not a column of the comparison here; that subset is named because a
population no reader can identify is the defect this section is about. Of 60 pairwise comparisons, 10 reverse in point estimate,
no population-by-method interaction separates from zero, and none survives a Holm correction. The
design detects an interaction of $0.0661$ at 80\% power, so it excludes large population effects
and not moderate ones. Two populations is a weak design and we do not offer it as a general
claim; it is reported because the alternative is to say nothing about a population anyone can
see is not random.

\section{The worked example in full}
\label{sec:si-case}

The example is reported on both drawings of the substrate, because the drawing decides which
templates can fire on it and the corpus's drawing is not the one a chemist would use. The stored
form is the 4-imino-2-hydroxy lactim
\texttt{N=c1ccn(C2OC(CO)C(O)C2(F)F)c(O)n1}; the form a chemist draws, which is also the fixed
point of the declared standardiser, is \texttt{Nc1ccn(C2OC(CO)C(O)C2(F)F)c(=O)n1}. The
annotation is looked up under the corpus string in both runs, so the two are scored against one
reference set and are paired.

\input{si_table_case}

\section{What retraining moves}
\label{sec:si-retrain}

Every recall figure in the manuscript comes from one trained checkpoint, and the paired bootstrap
intervals quantify the substrate population rather than the training. To give the registered
effects a scale, the generator and the filter were retrained under \numSeedN{} seeds and the
whole pipeline re-evaluated on the full test split: macro recall at a budget of 15 is
\numSeedMacroMean{} with a standard deviation of \numSeedMacroStd{}, and micro
\numSeedMicroMean{} with \numSeedMicroStd{}.

That study ran at an earlier operating point than the configuration the manuscript reports, so
its level is not comparable with those tables, and whether its spread carries across operating
points was assumed rather than shown. It does not: retraining the generator and the filter under
the same \numSpreadSeeds{} seeds at the configuration the manuscript does report gives a standard
deviation of \numSpreadInteractiveSd{} for the interactive arm at a budget of fifteen, on a mean of
\numSpreadInteractiveMean{}, and \numSpreadExhaustiveSd{} for the exhaustive arm on
\numSpreadExhaustiveMean{}, ranging from \numSpreadInteractiveSdmin{} to
\numSpreadExhaustiveSdmax{} across budgets. Two arms at one operating point are further apart than
the two operating points were, so the spread is a property of the arm and the budget as much as of
the training.

Read against the arm whose knob each effect is, the rank fusion of P1 is \numSpreadPOneInsd{} times
the interactive arm's spread, the pool cap of P2 \numSpreadPTwoInsd{} times it, and the rule budget
of P3, at $+\numHOneZeroBought$ on validation, \numSpreadPThreeInsd{} times it rather than inside
it, which is what the earlier operating point's figure had suggested. That last reading is the one
running this work's way and is bounded accordingly: the effect is measured on the validation draw
and the spread on the comparison set, so the ratio is indicative and not a test. Against the
exhaustive arm's spread the same effect would sit inside the noise, which is the other reason to
read it as indicative.

\section{Runtime methodology}
\label{sec:si-runtime}

\paragraph{Correcting the speed-up for machine load.} The bounded and unbounded arms did not run
under the same load. The enumeration step is identical between them, so the ratio of its medians,
\numHOneFiveLoad{}, measures load alone. Applying that correction to the step the budget changes,
standardising the survivors, whose medians are \numHOneFiveStdunbounded~s unbounded against
\numHOneFiveStdbounded~s under the budget, leaves \numHOneFiveAttributable$\times$ attributable
to the budget itself. Carried to the whole arm it puts the median at
\numHOneFiveMedianUnderload~s under the other arm's load, inside the registered target.

\paragraph{A safeguard the budget needed.} The tautomer enumeration budget lives in a
module-level singleton shared with the matching key, so lowering it would have moved what counts
as a hit at the same time as it moved the cost. A private enumerator was introduced for the
bounded arm and the match keys were verified unchanged before the run.


Table~\ref{MS-tab:modes} gives the exhaustive mode's non-completion rate as \numCostUnfinished{} of
\numCostSampled{} sampled validation substrates, \numCostUnfinishedsharePct{}, at a deadline of
\numCostDeadline{} seconds, in a paper that says a service must publish the tail as well as the
median. Those \numCostSampled{} are not a random sample of the \numCostPopulation{}, and the draw
matters to how the rate reads: the substrates are ordered by heavy-atom count, every
\numCostEvery{}rd is taken, and the largest \numCostTail{} are then added outright, because the
non-completions live at the top of the size range. They do:
\numCostTailunfinished{} of the \numCostUnfinished{} are among those \numCostTail{}, and over the
\numCostSystematic{} sampled substrates outside them the rate is \numCostSystematicunfinished{},
\numCostSystematicsharePct{}. So \numCostUnfinishedsharePct{} is an upper bound built on purpose
rather than an estimate of the population's rate. The systematic part is an exact one-in-%
\numCostEvery{} sample of everything outside the largest \numCostTail{}, which are measured
entire, so the population figure follows without a model: about \numCostEstimated{} of
\numCostPopulation{}, \numCostEstimatedsharePct{}. Both are reported because the sampled rate is
the one Table~\ref{MS-tab:modes} prints and the population rate is the one a service would meet.
Either way it is a validation-timing figure at that deadline and not a property of the comparison. The two are not
in conflict: the timing sweep kills a worker that exceeds the deadline, and the run that produced
the predictions imposes none, so on the test split the same arm was allowed to finish and records
\numScoredFailed{} failures over \numScoredSubstrates{} substrates. What the rate says is how
often the exhaustive mode would miss a ten-minute deadline, not how often it fails.

Every timing in the paper is a cold measurement. A cache makes a repeated substrate run 16 to 47
times faster, so any timing not taken cold measures a mixture of two regimes.

Standardisation is the binding constraint on the generator, at 94 to 99\% of cold generator time.
Three other candidates were tested against it and rejected by measurement: the pairwise filter's
maximum-common-substructure step, which does not bind because the generator outruns the filter a
hundred to one; rule application, which applies all \numBankParses{} parseable templates to a
109-atom molecule in $5.2$~s, yielding 20{,}655 products at no more than $0.58$~s per rule; and
substrate size as an axis, which fails as a predictor because two 43-atom substrates take
$64.7$~s and no completion respectively.

Two of the registered predictions address the constraint. Moving standardisation to surviving
candidates alone (P8) reduces enumeration by a factor of $13.9$, but standardising the hundred
survivors then becomes 72\% of the new arm, so the whole arm gains only $2.95\times$ against a
registered $10\times$: removing 94 to 99\% of the work does not remove 94 to 99\% of the time.
Adding a tautomer enumeration budget of \numHOneFiveBudget{} passes both halves of its
prediction.

That budget lives in a module-level singleton shared with the tautomer matching key, so setting
it globally would silently move what counts as a hit. It is applied through a private enumerator
instead: the global setting is untouched, and matching keys on 96 molecules are identical with and
without it.

\section{Comparators}
\label{sec:si-comparators}

SyGMa 1.1.0 is taken from the installed module and run with the scenario
\texttt{[[phase1,\,1],}\allowbreak\texttt{[phase2,\,1]]}, one cycle of each ruleset, which is
the worked example in that version's own documentation. It is stated because it is the one comparator knob that sets how many candidates a
system may emit, and a paper arguing that such a knob must be declared should declare it.

That scenario is a knob of exactly the kind this paper asks other work to declare, so it is swept
rather than only stated. SyGMa's engine composes: phase II fires on the products of phase I, so
the deployed scenario is a two-step run and not one application of each ruleset. Applying each
ruleset once to the parent and composing nothing reaches \numSygdepthOncepass{} of the references
against \numSygdepthDeployed{} for the deployed scenario, so the composition step buys
$+\numSygdepthEngine$, 95\% CI $[\numSygdepthEnginelo, \numSygdepthEnginehi]$. Against this
work's own uncapped single pass the difference is \numSygdepthGapdeployed{} at the deployed
setting and \numSygdepthGapmatched{} depth-matched: the comparison of the two banks does not turn
on which of the two SyGMa is run at.

The other direction is the one that bears on the only wide-budget lead this work still claims, and
it is swept too. Running SyGMa at two cycles of phase I against one of phase II takes its mean
emission from \numSygscenDeployedemitted{} candidates to \numSygscenDeepemitted{}, more than five
times what this work's exhaustive arm returns, and its recall at a budget of 50 from
\numSweepSygmaFiveZero{} to \numSygscenDeeprecall. The lead survives it:
$+\numSygscenDeepGap$ $[+\numSygscenDeepLo, +\numSygscenDeepHi]$ against
$+\numSygscenDeployedGap$ $[+\numSygscenDeployedLo, +\numSygscenDeployedHi]$ at the deployed
setting, both intervals excluding zero. \numSygscenUnfinished{} substrates did not finish the
deeper scenario inside a ninety-second deadline and keep their deployed lists, which can only
favour SyGMa's arm in the comparison rather than harm it. That deadline is wall-clock, so this
count and the mean emission it produces depend on how loaded the machine is: a re-run on a quieter
machine finished a substrate that had previously timed out, which lowers the count and raises the
mean emission, since that substrate then contributes the deeper scenario's longer list. Every
recall, every gap and every interval in this sweep was identical across the two runs. The fields a
re-run may move are named in the artifact, so a check holds the rest to equality rather than
reading any movement as noise.

\paragraph{MetaPredictor's beam, swept the same way.} The other comparator whose emission is a
declared setting is MetaPredictor, and its flatness from $k=15$ is the clearest case in this work of
a budget that has stopped measuring ranking: \numShortMetapredictorOneFive{} of its
\numPopulationN{} lists are shorter than that budget. Decoding the same substrates at a wider beam
takes its mean output from \numMpbeamDeployedemitted{} candidates to \numMpbeamWideemitted{} and
its recall from \numSweepMetapredictorThreeZero{} at both thirty and fifty to
\numMpbeamWiderecallThreeZero{} and \numMpbeamWiderecallFiveZero{}; at fifteen it reaches
\numMpbeamWiderecallOneFive{}. Decoding at the wider setting lost \numMpbeamWidemissingWord{} of the
substrates, so the two rows are scored on the same population under the same criterion, and the
prediction equal to the substrate is dropped from both.

What that does to the margin is the point of running it. At thirty the exhaustive arm leads by
$+\numMpbeamDeployedThreeZeroGap$ $[+\numMpbeamDeployedThreeZeroLo,
+\numMpbeamDeployedThreeZeroHi]$ at the deployed beam and by $+\numMpbeamWideThreeZeroGap$
$[+\numMpbeamWideThreeZeroLo, +\numMpbeamWideThreeZeroHi]$ at the wide one, both excluding zero.
At fifteen the deployed reading is $+\numMpbeamDeployedOneFiveGap$
$[+\numMpbeamDeployedOneFiveLo, +\numMpbeamDeployedOneFiveHi]$ and the wide one is
$+\numMpbeamWideOneFiveGap$ $[\numMpbeamWideOneFiveLo, +\numMpbeamWideOneFiveHi]$, which does not
exclude zero. So the head-of-list margin against MetaPredictor is a statement about the beam it was
run at, and the depth margin is not.

Both rows are printed rather than the one that favours this work. A paper arguing that an
undeclared emission budget reorders a published table cannot report its own comparison at one
budget and call the ordering a property of the systems, and the matched-length control reaches the
same verdict from the other direction by truncating the longer list rather than lengthening the
shorter one. The two settings are the tool's own documented decode configurations, not settings
invented here.

The comparison is reported at the documentation default because that is the setting a user gets,
and the sweep is reported because a paper asking others to declare an emission knob has to turn
its own.

Only \numComparatorsVersionedWord{} of the \numComparatorsNWord{} comparators expose a version
string at all. SyGMa reports 1.1.0 from the installed package and BioTransformer 3.0 carries the
build stamp \numComparatorsBiotransformerBuild{} in its jar, whose digest is pinned. MetaTox is a web service that publishes
no version to a user, and MetaPredictor is a source checkout with no release tag. For those two
the record is what can be pinned rather than what would be convenient to claim: the exact
submission set, the returned predictions frozen per substrate, their digests, and the date each
entered this repository, which bounds when the run happened. Those dates are
\numComparatorsMetatoxDate{} for MetaTox and \numComparatorsMetapredictorDate{} for
MetaPredictor, against \numComparatorsSygmaDate{} and \numComparatorsBiotransformerDate{} for the
other two. A comparison against a service cannot be made reproducible by the party running the
comparison; declaring what it can pin, and naming what it cannot, is the whole of what is
available, and the shortfall belongs in the record rather than out of it.

What the MetaTox arm is, item by item: a single file of \numPopulationN{} structures submitted to
the service on \numComparatorsMetatoxDate{}, and the file it returned, frozen per substrate and
pinned by digest. That is the whole of what this repository holds about the run. The service
publishes no version to a user, the returned file records no setting, and no setting was chosen by
us, so nothing further about how those predictions were produced can be established from what is
held here. That is a shortfall and it is named as one rather than filled in: this paper argues
that a comparator's emission knob must be declared, and for the incumbent it sets out to replace
it declares a submission and a response and no knob. It is also why the arm cannot be re-run here
and why the sweep applied to SyGMa's scenario has no counterpart for it.

What can be said beyond that is about review and not about settings, and the two should not be
confused. The first author of MetaTox \citep{Rudik_2017} is a co-author of this paper and read
the arm it is compared in, so the question to ask of any comparison, whether the comparator was
run in a configuration its own authors would recognise, has been put to one of them for the
incumbent this work sets out to replace. That is a fact about the method and not a disclosure:
the paper argues for replacing MetaTox, so any interest the relationship could create runs
against this work rather than for it.

GLORYx is not a column here, and the honest reason is that its predictions on these substrates
were not obtained: no distribution of it is held in this repository and none was sought. Whether
it could have been run is a question about effort and about its interface that we did not settle,
so the absence is not defended as unavoidable.
Its reference set is also one of the four sources this corpus is assembled from, so its
development data sits inside our corpus, and Section~\ref{MS-sec:populations} states that such
overlap flatters a comparator. That second fact is not what excludes it. BioTransformer stands in
the same relation to MetXBioDB, \numAssemblyMetxsharePct{} of whose pairs are inside this
corpus, and it carries a column with its advantage stated and uncorrected; excluding one system
for an overlap while seating another with a comparable one would be a rule applied in whichever
direction suited the result. The overlap is therefore reported for both and corrected for
neither, and GLORYx's absence stands on obtainability alone. It is a standard arm in this
journal's recent benchmarks of this tool class \citep{Gao_2026}, so the absence is a real
weakness of this comparison rather than a neutral one, and it is recorded as such in the
manuscript's Limitations.

BioTransformer is pinned by digest rather than by version string, because ``3.0'' names several
builds: jar \texttt{c70cad91}, 115{,}415{,}197 bytes, upstream commit \texttt{6432cf88} of
2023-05-25, its \numBtTemplates{} templates loaded at freeze time rather than taken from a
constant. That count is every template the jar loads across all its rule databases and is not the
one Section~\ref{sec:si-bank} partitions on: the core rule file this repository reads holds
\numThirdpartyBiotransformerRead{} of them, and \numThirdpartyBiotransformerShipped{} of those
are in this bank character for character.

It carries a column. Supplying no ranked predictions would not have excluded it, since that
does not separate it from MetaPredictor, whose list is also unranked and shorter than the budget on
nearly every substrate. Running it takes one thing worth recording for anyone who tries: the
native InChI library its distribution caches is built for one processor architecture, so on
another the tool aborts at load, and an x86-64 Java 8 runtime resolves it. It is the comparator a
reader of the rule bank most wants, since \numThirdpartyBiotransformerShipped{} of its templates
are inside that bank, so its column measures how much of this work's reach is BioTransformer's
reach under another name.

It is run at \texttt{allHuman} for one step, which is one application of its own rules and the
closest counterpart to what this work's arms do; it is not a claim that every arm is allowed the
same enzymatic depth, since SyGMa's deployed scenario composes a phase-one and a phase-two ruleset
and a mined template may itself perform more than one edit. Its file order is its ranking, which
is the treatment MetaPredictor's unranked list already receives. It emits \numBtarmEmitted{} candidates
per substrate and returns nothing for \numBtarmSilent{} of \numPopulationN{}. The exhaustive arm
leads it by $+\numBtarmGapThreeZero$ $[+\numBtarmGapThreeZeroLo, +\numBtarmGapThreeZeroHi]$ at a
budget of thirty. Cut to BioTransformer's own list length on every substrate, the same control the
other comparators receive, the difference is $\numBtarmMatchedgap$ $[\numBtarmMatchedlo,
+\numBtarmMatchedhi]$ over \numBtarmMatchedslots{} slots, an interval covering zero and running
the other way. The
wide-budget gap against BioTransformer is therefore an emission gap and not an ordering one, which
is what a bank sharing \numThirdpartyBiotransformerShipped{} of its templates should produce.

Two things follow for the reading of the rest. The declared family the family-wise correction is
computed over names its three comparators and was fixed before this arm existed, so it is
unchanged: BioTransformer's contrasts are reported with their own intervals and are not folded
into a correction declared without them. A family fixed in advance is not widened after the fact,
which is the whole of what declaring one buys, and the objection that the paper then prints more
tests than its correction covers is answered by measurement rather than by the argument: running
the same Holm procedure over all \numHolmWidetests{} contrasts the paper prints removes
\numHolmWideremovesWord{} of the cells the declared family licenses, the exhaustive arm's trail
against SyGMa at a budget of five, which is a trail and not a lead and is reported either way. And the deeper scenario is not swept for this comparator
as it is for SyGMa. That asymmetry was decided after the matched-length control had been read,
which is worth stating because it is exactly the kind of choice this work asks other papers to
declare: the control removes list length as a variable and finds no separation, so a scenario that
emits more would narrow a gap this paper does not claim as an ordering result and would change no
verdict here. A reader who judges that insufficient should read the absence as unforced.

Its templates are also measured for reach, which is a different quantity from its recall, on the
same \numBtN{}-substrate subset drawn for the earlier MetaTrans evaluation
(Section~\ref{sec:si-population}): they reach \numBtReach{} of the
references there, 95\% CI $[\numBtLo, \numBtHi]$, against \numBankRules{} templates of ours
reaching \numBtOurreach{}. A reach figure and a ranked recall are not the same quantity and they
are not placed in one table.

The substrate was handed to BioTransformer as the corpus stores it, as it was to SyGMa and
MetaPredictor. Handed the natural tautomer instead, the same arm reaches
\numBtarmDrawnrecallThreeZero{} against \numBtarmRecallThreeZero{}, a difference of
$\numBtarmDrawingdelta$ at a budget of thirty. That is the second comparator for which both
drawings could be run and the two disagree in direction: the drawing that costs SyGMa recall costs
BioTransformer nothing, which is what a tool that standardises its own input would do.

Four systems published in 2025--26 could not be run on our split as released. LAGOM
\citep{Larsson_2025} releases training code under Apache-2.0 but no fine-tuned checkpoint, so
comparing would mean comparing against our own reimplementation. DeepMetab \citep{Zhou_2025}
releases weights and site prediction but its reaction module was withdrawn by the author for
copyright reasons, leaving only Python 3.7 bytecode. DeepCYP \citep{Zhou_2026} offers a web form
and no models or API, so a comparison would mean submitting substrates one at a time through a
form.

MetaTox is also a service rather than a package and was run, so the rule that separates them has
to be stated and it is a weak one. It is not reproducibility: neither service's settings are
recorded here, and Section~\ref{sec:si-comparators} says as much for MetaTox. It is not
availability: both are reachable. It is the cost of submission, that MetaTox accepts a file of
\numPopulationN{} structures and returns a file where DeepCYP takes one substrate at a time.
That is a statement about our effort and not about either system, and it is the kind of reason
this paper asks other work not to leave implicit, so it is written here for a reader to weigh.
A reader who judges it insufficient should read DeepCYP's absence as unforced, and the same
applies to GLORYx, whose distribution was not sought. Metabolite-Gen \citep{Chavan_2026} is a preprint with no repository we could
find. The list is closed by a rule: a comparator that could not be obtained is reported as
unavailable with its reason, is not dropped silently, and the list is not extended after the
first run.

\section{Provenance and reproduction}
\label{sec:si-prov}

\texttt{results/four\_method\_291.json} defines the comparison population, gates the MetaTox
column and carries the deployed interactive arm from the pools the comparison itself is read from,
reproducing Table~\ref{MS-tab:sweep} in every cell of both. It also records, beside that arm, the
one it replaced: an early dump ranked by the product of the two component scores over a narrower
pool, which reached \numFourmethodSupersededFiveZero{} at $k=50$. No number in this paper derives
from that column, and it is kept rather than deleted so a reader who meets it in an older copy can
tell which arm they are looking at. Table~\ref{MS-tab:sweep} is supplied by
\texttt{deployment\_table.json}, which carries a gate asserting the two agree on MetaTox.

\numProvPinned{} artifacts are pinned. Most record the script that wrote them, the SHA-256 of
that script's source, and the commit it ran at; a sweep recomputes the hash of each producer and
refuses the artifact if that producer has changed. \numProvPinnedcurrent{} of the
\numProvPinned{} are current.

A pinned producer is not the same as a pinned model. Candidate pools record both checkpoints that
scored them with their digests; the pool sets frozen before that field existed carry no such
record, and for those the run is established by reproduction: their generator and filter scores
are reproduced exactly by one run's checkpoints and by no other pair held here, so both stages of
the released system come from one run. The analysis scripts default to it, a test refuses any
producer of a pinned artifact that names another, and the gate over the artifacts the
manuscript's numbers come from fails on any that names a checkpoint outside it, because a default
that is not the deployed setting reproduces something nobody ran. That test reads the shell
scripts too, and the two ways a path is written in them, which is how it now sees the one that
built the standardised pools: it named the filter of a run this repository no longer ships, so
the sweep over how the substrate is drawn compared two models rather than two drawings, and its
figures here are from the rebuild.

The guarantee that matters is narrower than the pinned set and is checked by a gate rather than
asserted. The numbers in this paper come from \numProvSources{} of those artifacts, established
by running the number generator with its artifact reader instrumented, so the count is of files
actually opened and not of matches in the source. Of the \numProvSources{}, the sweep finds
\numProvSourcesunpinnedWord{} unpinned and \numProvSourcesunverifiableWord{} whose producer it
cannot identify. Of the rest, \numProvSourcesinferredWord{} predate stamping and are verified by
recovering their producer's source from the history rather than from a recorded digest, which the
sweep labels as inferred; that is a weaker check and is counted separately for that reason.
A further \numProvSourcesexemptWord{} are exempt with a stated reason, each an output of the checking
apparatus itself, which cannot verify its own inputs without circularity.

Which model produced an artifact is checked separately, because a producer whose source has not
moved can still have been pointed at a checkpoint nobody deployed, and one was: the worked example
was ranked by a filter from a neighbouring training run for as long as the example existed, and its
own artifact said so in plain text. \numProvSourceswrongmodel{} of the \numProvSources{} now name
a checkpoint outside the deployed run, and the gate fails on any that do. That check has a blind
spot worth naming: an artifact that records no checkpoint passes it without being examined. For
\numProvSourcesestablishedWord{} of the \numProvSources{} the identity is on the record even
though that file does not carry it, established the way the deployed pair itself was: scoring the
artifact's own candidates with every trained checkpoint held here and finding that one reproduces
them exactly and no other reproduces them at all. \numProvSourcesnocheckpoint{} remain in the
blind spot, their producer loading a model and nothing recording which; for those the assurance is
weaker and is of a different kind, that every such producer's default is the deployed pair and a
test refuses any that names another. The gate fails the build
if any of the first two counts is not zero, and unpinning one artifact makes it fail, which is how
we know it can.

The identification itself had a blind spot of a different kind, and it is the one that let the
standardised pools through. It enumerated pool directories by a filename pattern and reported on
whichever ones matched, saying nothing about those that did not, so four of the thirteen a
published number is read from were never examined -- among them the pool the interactive arm's
whole column comes from, and both standardised pools. A check that reports on what it finds and
is silent about what is missing cannot fail on the thing that is missing. It now refuses when a
declared directory yields no shard it can read, the readers of those pools refuse a shard that
does not record the released pair, and the invariant that exposed it is checked on every run: a
substrate the standardiser does not move is the same molecule in both arms, so the two must
return the same list on it.

Bringing those artifacts inside the guarantee meant re-running their producers, which is a rerun
and not a relabel, so what moved is reported. One number moved. The train split's clean triple
count is \numSplitTrainTriples{} where the committed artifact recorded three more: three records
of \texttt{train.sdf} that the RDKit version used here no longer parses, which the loader reports
and which affect no substrate, no annotated pair and no figure in this paper. Every other field of
every re-run artifact is byte-identical to what was committed, including the leakage audit's
overlap counts, the contamination audit and the population-defining comparison. That is the
evidence that the unpinned state was harmless, and it is worth having in a form stronger than an
assurance.

Two limits of the apparatus are ours to state. First, and materially: the stamp digests the
producer script's own bytes and nothing else, so a change anywhere in the package (the code
that computes the tautomer keys, the matcher, the recall) invalidates no artifact and trips no
gate. The split manifest pins the data and the sweep pins the producer; between them sits the
library, and it is pinned by neither. Second, and narrowly: most stamped artifacts record a dirty
working tree at write time. Verification resolves by the recorded source digest, which is
independent of the commit, so the producer is identified regardless; what the flag says is that
the surrounding tree was not clean, not that the producer is unknown.

The guarantee also covers the pinned set and not the directory: of \numProvFiles{} files under
\texttt{results/} and everything below it, \numProvUnstamped{} carry no stamp and
\numProvChanged{} were written by a producer that has since changed. None of those is among the
\numProvSources{} the numbers come from, which the gate is what establishes. The sweep reaches
below the top level because most of what a published number is actually made of lives there:
the candidate pools are written one shard at a time into their own directories, and a sweep that
did not enter them would leave the files a curve is assembled from outside every check it makes.
An unstamped file there is not therefore unaccounted for. A producer records the files it read by
path and digest, so the guarantee extends one hop past the pinned set: \numProvNamedinputs{} of
the files below the top level are named as an input by a pinned artifact, and a change to any of
them makes that artifact fail rather than pass.

A split manifest pins by digest the triple files, the substrate sets, the evaluated test set, the
rule bank and every third-party artifact, and a verify mode recomputes and names any leaf that has
moved.

Numbers reach the page through macros generated from pinned artifacts: \numProvMacrosbody{}
distinct macros in the manuscript and \numProvMacrossi{} here. The claim is not that every
number does. \numProvHandtyped{} measurements are typed by hand, most of them in this document,
and they are enumerated in the checker itself rather than folded into a wildcard, so a number
typed by hand that is not already on that list still fails the check, and the list is the exact
statement of what remains to be wired. A checker refuses any other numeric literal in either
document, refuses any hand-typed pointer between the two documents in either direction, and
writes the three counts this paragraph quotes. Every such pointer resolves through
\texttt{xr} against the other document's own labels, so a wrong one prints \texttt{??} and
fails the build rather than compiling silently. A hand-typed pointer cannot: nine of them here
were wrong at once when the tables were reordered, one badly enough to direct a search to the
wrong table, which is why none is permitted.

One file in the repository holds source structures rather than keys: the evaluated test
substrates, released as SMILES because reproducing the comparison means running a predictor on
one. Their annotated metabolites are not released that way. A single key would have fixed the
matching criterion at that key, since canonical equality, the stereochemistry-blind first block
and Tanimoto similarity each need the reference structure, and the sweep this paper is largely
about could then be reproduced under one criterion only. What is released instead is the set of
descriptors those criteria are decided by, one per reference, which is enough for all
\numCritCriteria{} of them and is not the structure
(Section~\ref{sec:si-corpus}). \texttt{reproduce\_from\_descriptors.py} rebuilds the whole
comparison from them under every criterion, reading no corpus structure at any point, and its
default-criterion column matches the one the manuscript prints at every budget. The substrates are
the residue of the problem and the choice to ship them is stated here rather than settled by the
file's existence.

The train and validation candidate pools are deposited at \ZenodoDoi{}. Every substrate and
every annotated metabolite in them is replaced by its tautomer-canonical InChIKey before the
archive is built, so the deposit holds no source structure; the candidate structures beside the
keys are this work's own output and are released as they are. The transform is part of the
deposit tool and its counts are recorded in the manifest (how many substrates went in, how
many came out, how many could not be keyed and how many keys collided) because a silent drop
there would be a silent loss of population. The manifest also pins the SHA-256 of both the
archive and the file inside it together with the record counts, so a download can be verified
against the version used here. The tool refuses to build an archive from a pool whose substrates
were not keyed, and refuses to publish one at all without a licence: a Zenodo record cannot be
withdrawn once published, so the last gate before one exists is that it carries nothing that
cannot be redistributed.

\bibliography{refs}

\end{document}
