\begin{table}[h]
\centering\small
\begin{tabular}{llll}
\toprule
quantity & mode & population & mean \\
\midrule
pool before deduplication & interactive & validation draw, 293 & 17.0 \\
pool before deduplication & exhaustive & validation draw, 293 & 669.9 \\
pool the emission rule reads & interactive & comparison set, 291 & 16.31 \\
list returned, deduplicated and capped & interactive & validation draw, 294 & 16.5 \\
list returned, deduplicated and capped & exhaustive & validation draw, 293 & 98.0 \\
list returned, deduplicated and capped & interactive & comparison set, 291 & 15.6 \\
list returned, deduplicated and capped & exhaustive & comparison set, 291 & 98.1 \\
list returned, one substrate & interactive & the worked example & 18 \\
list as the comparator emits it & MetaTox & comparison set, 291 & 33.6 \\
list as the comparator emits it & SyGMa & comparison set, 291 & 72.98 \\
list as the comparator emits it & MetaPredictor & comparison set, 291 & 10.75 \\
list as the comparator emits it & BioTransformer & comparison set, 291 & 10.7 \\
the same list truncated at $k=50$ & MetaTox & comparison set, 291 & 30.9 \\
the same list truncated at $k=50$ & SyGMa & comparison set, 291 & 40.5 \\
the same list truncated at $k=50$ & MetaPredictor & comparison set, 291 & 10.7 \\
the same list truncated at $k=50$ & BioTransformer & comparison set, 291 & 10.7 \\
slots the matched-length control allows & MetaTox & comparison set, 291 & 33.23 \\
slots the matched-length control allows & SyGMa & comparison set, 291 & 58.37 \\
slots the matched-length control allows & MetaPredictor & comparison set, 291 & 10.75 \\
slots the matched-length control allows & BioTransformer & comparison set, 291 & 10.7 \\
\bottomrule
\end{tabular}
\caption{Every candidate count this work reports, for every arm. They differ because they are measured at different stages of the pipeline and on different populations, not because any of them is an estimate of the others: a list as a system emits it, the same list truncated at the widest budget the sweep reads, and the slots the matched-length control allows are three quantities and the first is the one that describes what a user is handed.}
\label{tab:si-counts}
\end{table}
