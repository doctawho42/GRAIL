numpy>=1.24,<2.0
pandas>=2.2,<3.0
# Pinned exactly, not floated. The evaluation protocol's matching criterion runs through
# RDKit's tautomer canonicalisation, whose canonical form is not stable across releases, so
# the reported numbers are reproducible only at this version. Do not relax without re-running
# scripts/gloryx_criterion_ladder.py and scripts/factorize_recall.py and updating the paper.
rdkit==2022.9.5
scikit-learn>=1.5,<2.0
scipy>=1.11,<2.0
torch>=2.4,<3.0
torch-geometric>=2.4,<3.0
matplotlib>=3.9,<4.0
tqdm>=4.65,<5.0
pyvis>=0.3,<1.0
Pillow>=11.0,<13.0
PyYAML>=6.0,<7.0

# Optional: only for plotting helpers, which import it lazily or under try.
# seaborn>=0.13,<1.0
