# Legacy Scripts

These scripts are preserved as historical research utilities.

They are not part of the supported package API, are not exercised by the
regular test suite, and should not be used as the default workflow entrypoints.

Prefer:

- `grail` CLI
- `scripts/run_*.py`
- `grail_metabolism.workflows`

The wrappers at the repository root only exist for backward compatibility.
